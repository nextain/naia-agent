{
  "name": "naia-agent",
  "version": "0.0.0",
  "private": true,
  "description": "AI coding agent runtime — loop, tools, compaction, memory, LLM routing",
  "type": "module",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "smoke:anthropic": "tsx scripts/smoke-anthropic.ts"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@naia-agent/providers": "workspace:*",
    "@naia-agent/types": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
