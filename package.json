{
  "name": "new-naia-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "naia-agent(brain) 헥사고날 이식 — UC1 brain 파이프라인",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run src/test/*.test.ts",
    "test:watch": "vitest src/test/*.test.ts",
    "test:report": "vitest run src/test/*.test.ts --reporter=verbose"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.179",
    "@grpc/grpc-js": "^1.14.4",
    "@grpc/proto-loader": "^0.8.1",
    "@nextain/naia-memory": "file:../../naia-memory",
    "js-yaml": "^4.2.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
