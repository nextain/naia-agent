{
  "name": "@nextain/agent-adapter-shell",
  "version": "0.1.0",
  "description": "Shell SubAgentAdapter — wraps an external CLI as child_process with NaiaStreamChunk events. Phase 1 fallback for opencode-cli.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "default": "./dist/index.js"
    }
  },
  "sideEffects": false,
  "files": ["dist", "src"],
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nextain/naia-agent.git",
    "directory": "packages/adapter-shell"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@nextain/agent-types": "workspace:*",
    "@nextain/agent-observability": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
