# Cross-Review — 2026-05-21T05-33-45

**Prompt size**: 6857 chars
**AIs queried**: 4
**Valid responses**: 2/4

## glm — ok (100285ms)

```
We are reviewing the code fixes for the R1 adversarial findings. The fixes are in commit `9c82688` and we are to check if they solve the claimed problems without considering the R2 measurement numbers.

We'll go through each fix (S1 to S10) and answer the review questions for each.

### S1: `extractVisibleContext()` ignored real recap → `FixtureResult.recapContent` exposed; harness consumes it

**Review Question 1**: Does exposing `recapContent` actually fix the unfairness? What happens when `recapContent === ""` (no compaction or no-op prune) and `last !== undefined`? Could the fallback branch leak the old unfair behaviour?

**Analysis**:
- The fix adds a `recapContent` field to `FixtureResult` and returns it from the runner.
- In `mini-bench-judge.ts`, the visible context for `reactive-vercel` is set to `recapContent` (if present) and for compact strategies, it uses `recapContent` and appends the tail.
- The old unfair behavior was that the `extractVisibleContext` function ignored the recap (pruned messages) and only used the last few turns. Now, the harness uses `recapContent` which is built from the pruned messages (if any) and then appends the tail for compact strategies.

**Potential Issue**:
- When `recapContent` is empty (meaning no pruning happened, so the entire history is available) and `last !== undefined` (meaning we are in a compact strategy), the code appends the tail (from `last` to current turn) to the `recapContent` (which is empty). This would result in the tail being appended to an empty string, which is the same as just the tail. But note: the entire history is available, so why are we only showing the tail? This might be the intended behavior for compact strategies: to show the recap (which is empty) and then the tail (the recent turns). However, the recap being empty means that the entire history was kept, so the tail is actually the entire history? Not exactly: the tail is from `last` to current turn, and `last` is the turn after which the compaction happened. But if no pruning happened, then `last` might be 0? We need to check the code.

Looking at `runner.ts`:
- The `prepare` function (which does pruning) returns `undefined` if no pruning is done. Then, in the runner, `recap` is set to `""` initially. If `prepare` returns `undefined`, then `recap` remains `""`.
- Then, when building the `FixtureResult`, `recapContent` is set to `recap` (which is `""` in the no-prune case).

In `mini-bench-judge.ts` for compact strategies:
```ts
if (isCompactStrategy && last !== undefined && recapContent.length > 0) {
  const tail = fixture.turns.slice(last, currentTurn).map(...).join("\n");
  return `[after compaction at turn ${last}]\n${recapContent}\n\n${tail}`;
}
```
But note: the condition is `recapContent.length > 0`. So if `recapContent` is empty, this branch is skipped. Then what? The code above that branch for `reactive-vercel` also requires `recapContent.length > 0`. So if `recapContent` is empty, then the visible context is built by the default branch? Let's see the default branch:

```ts
const last = ...; // the turn after which compaction happened (if any)
const visible = ...; // built from the entire fixture turns? Not exactly.

// The code for building visible context:
if (strategy === "reactive-vercel" && last !== undefined && recapContent.length > 0) {
  // ... uses recapContent
} else if (isCompactStrategy && last !== undefined && recapContent.length > 0) {
  // ... uses recapContent and appends tail
} else {
  // Default: build from the entire fixture turns?
  visible = fixture.turns.slice(0, currentTurn).map(...).join("\n");
}
```

So if `recapContent` is empty, then the code falls back to building the visible context from the entire fixture turns (from 0 to currentTurn). This is the same as the old behavior? But note: the old behavior was that `extractVisibleContext` only used the last few turns? Actually, the old `extractVisibleContext` function was:

```ts
function extractVisibleContext(fixture: Fixture, strategy: Strategy, currentTurn: number): string {
  // ... it would use the entire fixture turns? Actually, it used the entire fixture turns but then truncated by the context window? 
  // But the problem was that it ignored the recap (pruned messages) and only used the last few turns? 
  // Actually, the problem was that it did not use the pruned messages at all? 
}
```

But in the new code, when there's no pruning (so `recapContent` is empty), we are building the visible context from the entire fixture turns (from 0 to currentTurn). This is the same as the old behavior? However, the old behavior was that the entire history was available? Then why was it unfair? The unfairness was that the recap (pruned messages) were ignored. Now, when there's no pruning, we are including the entire history. So that's correct.

But what if there was pruning? Then `recapContent` would be non-empty and we would use it. So the fix seems to work.

However, note that the condition for the compact strategies branch requires `recapContent.length > 0`. So if pruning happened but the recapContent is empty (which shouldn't happen because pruning should produce a non-empty recap?), then we fall back to the entire history. But if pruning happened, then the recapContent should be the pruned history? So it should be non-empty? Unless the pruning produced an empty string? That seems unlikely.

But the question is: when `recapContent === ""` and `last !== undefined` (meaning we are in a compact strategy and pruning happened? Actually, `last` is set to the turn after which compaction happened, which is set by the `prepare` function. If pruning happened, then `last` would be set to the turn index after which the pruning occurred. But if pruning happened and the recapContent is empty, then we skip the compact branch and use the entire history? That would be incorrect because we should be using the recap (which is empty) and then the tail? But the condition `recapContent.length > 0` fails. So we use the entire history? That would be the same as the old behavior? But the old behavior was that the entire history was available? Actually, no: the old behavior was that the entire history was available? But now, if pruning happened, we should only have the recap and the tail? But if the recap is empty, then we are effectively showing the entire history? That might be acceptable? But note: the pruning step might have removed some messages, so the entire history is not available? Actually, the pruning step removes messages and puts them in the recap. So if the recap is empty, then no messages were pruned? Then why is `last` set? This is confusing.

Looking at the runner code:
- The `prepare` function returns an object with `recap` and `last` (the turn index after which pruning occurred). If no pruning is done, it returns `undefined`. Then, in the runner, if `prepare` returns `undefined`, we set `recap` to `""` and `last` to `undefined`. So `last` is only set if pruning happened.

Therefore, if `last !== undefined`, then pruning happened and `recap` should be non-empty? Because the pruning function should return a non-empty recap? So the condition `recapContent.length > 0` should be true? Then the compact branch would be taken.

So the scenario of `recapContent === ""` and `last !== undefined` should not occur? Because if pruning happened, then `recap` is set to the pruned history (which is non-empty) and `last` is set. So the condition `recapContent.length > 0` would be true.

Therefore, the fix should be **FIXED**.

But note: the question asks about the fallback branch. The fallback branch (the else) uses the entire fixture turns. This is the same as the old behavior? But the old behavior was that the entire history was available? And now, when there's no pruning, we are using the entire history? That is correct. And when there is pruning, we use the recap and the tail? That is also correct.

So I think S1 is **FIXED**.

### S8: probe used reverse-engineered question → Schema: `task-accuracy.question?: string` + 5 KR fixtures backfilled + fallback stderr warning

**Review Question 2**: stderr warning is logged but doesn't block. Should it be an error? Are there fixtures where the implicit fallback question is semantically correct (so a warning would be noise)?

**Analysis**:
- The fix adds an optional `question` field to the `task-accuracy` probe type.
- If the probe has a question, it uses that. Otherwise, it falls back to the last user turn's content and logs a warning to stderr.

**Potential Issues**:
- The warning doesn't block the run. It's just a warning. Should it be an error? The original finding was that the probe used a reverse-engineered question, which might be incorrect. So if we don't have an explicit question, we are using the last user turn's content, which might be the correct question? Or might not? The warning is to alert the user that we are falling back. But if the fallback is correct, then the warning is noise. However, we don't know. The warning is appropriate because it indicates that we are not using the intended question (if any) and are using a fallback. The user can then decide to add the explicit question.

- Should it be an error? Probably not, because we can still run the benchmark and get a result. The warning is sufficient to alert the user that the probe might be using a fallback question.

- Are there fixtures where the implicit fallback is correct? Yes, if the probe is designed to use the last user turn as the question, then the fallback is correct. But the schema now allows an explicit question, so the user can set it to the last user turn if that's what they want. Then there would be no warning. So the warning only occurs when the probe does not have an explicit question and we are falling back. That is the intended behavior.

Therefore, the fix is **FIXED**.

### S9: `reactive-vercel` got double-tail → Distinct branch — pruned history as-is, no synthetic tail

**Review Question 3**: For `reactive-vercel`, the visible context is the `recapContent` (which `runner.ts` built from `llmMessagesToText()` on pruned messages). But pruning was a no-op (returned `undefined`) in the cookbook-default case — does `recapContent` then stay `""`? If so, what visible context does S9's branch produce?

**Analysis**:
- The fix for S9 adds a distinct branch for `reactive-vercel` that uses `recapContent` (if present) and does not append the tail.
- In the runner, if pruning is a no-op, then `recapContent` is set to `""` (because `prepare` returns `undefined` and then `recap` is set to `""`).
- Then, in `mini-bench-judge.ts`, for `reactive-vercel`:
  ```ts
  if (strategy === "reactive-vercel" && last !== undefined && recapContent.length > 0) {
    return `[reactive-vercel post-prune window]\n${recapContent}`;
  }
  ```
- If pruning was a no-op, then `recapContent` is `""` and `last` is `undefined` (because pruning didn't happen). So the condition `last !== undefined` fails? Actually, `last` is set to `undefined` in the runner when pruning doesn't happen. So the condition `last !== undefined` is false. Therefore, the branch is not taken.

- Then, what happens? The code falls through to the next condition (for compact strategies) or the default? The next condition is for compact strategies, which `reactive-vercel` is not? So it falls to the default? The default builds the visible context from the entire fixture turns (from 0 to currentTurn). That is the same as the old behavior? But the old behavior for `reactive-vercel` was that it used the entire history? So that's correct.

- However, the problem was that `reactive-vercel` was getting a double tail? How? The old code for `reactive-vercel` was:
  ```ts
  if (strategy === "reactive-vercel" && last !== undefined) {
    const tail = ...;
    return `[reactive-vercel post-prune window]\n${recapContent}\n\n${tail}`;
  }
  ```
  This would append the tail to the recap. But if pruning happened, then `recapContent` is the pruned history and the tail is the recent turns. But if pruning didn't happen, then `recapContent` is `""` and `last` is `undefined`, so the branch wouldn't be taken? Actually, the condition was `last !== undefined` without checking `recapContent.length>0`. So if pruning happened and `last` is set, then it would append the tail to the recap. But if pruning didn't happen, then `last` is `undefined` and the branch is skipped. So the old code didn't have a double tail in the no-prune case? Then why was there a double tail?
```

## opencode — infra-error (180048ms)

*Infra error*: exit null: ccuracy""[90m in src/fixtures · 16 matches[0m
[0m→ [0mRead src/fixtures/F005-tool-heavy.fixture.json
[0m→ [0mRead src/fixtures/F-KR-AB-01-abstention.fixture.json[90m [offset=120, limit=40][0m


## codex — infra-error (180017ms)

*Infra error*: empty stdout

## gemini — ok (153938ms)

```
S1: FIXED
  Verdict reason: Exposing `recapContent` in `FixtureResult` and using it in the `mini-bench-judge` harness correctly addresses the R1 finding where the harness was reconstructing visible context via heuristics, ignoring the actual output of the compaction strategies. Judges now see the real summarized/pruned state.

S8: FIXED
  Verdict reason: The addition of an explicit `question` field in `task-accuracy` probes (and backfilling it in KR fixtures) ensures the ensemble judges are evaluating the correct intent. The fallback logic in `mini-bench-judge.ts` provides backward compatibility while emitting a necessary warning.

S9: DOESNT_FIX_R1 / NEW_BUG
  Verdict reason: The fix in `mini-bench-judge.ts` (lines 142-144) correctly identifies that `reactive-vercel`'s `recapContent` is a full history, but it fails to account for messages occurring **after** the last compaction point. Since `recapContent` is only updated at `compactionPoints` (e.g., turn 8), and probes occur later (e.g., turn 14), the "no-tail" branch returns a stale context missing turns 9–14. This makes `reactive-vercel` appear broken in the R2 measurements.
  Concrete attack scenario: In `F-EN-TH-01-tool-heavy` with a compaction point at turn 8 and a probe at turn 14, the judge will see the pruned history up to turn 8 only. Any facts established in turns 9–14 (like the seafood reservation) will be missing from the judge's view, causing an unfair failure for the Vercel strategy.

S10: PARTIAL
  Verdict reason: `simulateContextWindow(visible, 2000)` successfully removes the "oracle" advantage for the `off` and `anthropic-native` baselines by capping them to a realistic budget (~500 tokens). However, the implementation in `mini-bench-judge.ts` (lines 182-184) exempts `reactive-vercel` from this cap.
  Concrete attack scenario: If `reactive-vercel` is a no-op (e.g., on plain-text fixtures), it falls back to the full transcript (line 152). Because it's not in the truncation filter (line 182), it receives the full transcript (> 2000 chars) while `off` is truncated. This gives the Vercel strategy an unfair oracle advantage over the baseline in non-tool-heavy scenarios.

S5: FIXED
  Verdict reason: Renaming the column to "Anchor-heuristic" in the report and code documentation clearly signals that this is a coarse sanity check rather than a primary ranking metric, addressing the "misleading" R1 concern.

S2: DOESNT_FIX_R1
  Verdict reason: While `F-EN-TH-01-tool-heavy` provides content that *could* be pruned, the harness `toLLMMessage()` (runner.ts:421) maps all turns to plain-string `content`. The `[thinking]` and `[tool_use]` markers are treated as opaque text. Vercel's `pruneMessages` (with `reasoning: "all"`) expects structured `reasoning` blocks; it will not strip these string markers. Thus, the new fixture still results in a no-op for the Vercel strategy in this harness.
  Concrete attack scenario: Running `reactive-vercel` against `F-EN-TH-01-tool-heavy` will show 0% reduction in reasoning/tool-call mass because the markers are never parsed into the `LLMMessage` blocks the pruner requires.

NEW_FLAWS_FOUND_IN_R2_CODE (S11+):
S11: Unfair advantage for `reactive-vercel` (no-op) over `off` in `mini-bench-judge.ts` due to lack of 2000-char truncation on the fallback transcript path.
S12: Double-tail in `runner.ts` (line 391) for `driftScore` calculation when `strategy === "reactive-vercel"`. Since the pruned recap already contains the tail, appending `fixture.turns.slice(-keepTail)` double-counts the most recent messages, skewing the drift metric.
S13: Context window mismatch: `runner.ts` (deterministic) shows only `keepTail` messages after a recap, while `mini-bench-judge.ts` (ensemble) shows all messages from `lastCompactionPoint` to `currentTurn`. This makes deterministic vs. ensemble results incomparable.
S14: Regression in `reactive-vercel` visibility (see S9) — missing tail messages after compaction points.
```
