# Cross-Review — 2026-05-21T06-00-18

**Prompt size**: 4199 chars
**AIs queried**: 4
**Valid responses**: 3/4

## glm — ok (137441ms)

```
We are reviewing the fixes for R3 verdict (REGRESSED) in R4 (commit d539e6e). We have three main fixes: S12, S18, and S2.

We'll go through each review question and provide a verdict (REMAINS, FIXED, WORSE, NEW_FLAW) for each.

### S12 (Missing Middle) Fix
The fix changes the tail slice to start at `lastCompactionPoint - keepTail` instead of `lastCompactionPoint`.

1. **Question**: With `tailStart = max(0, last - keepTail)`, what happens at probe `afterTurn` where `last === undefined` (no compaction yet)? The branch should return full transcript — verify it still does.
   - **Analysis**: In the code, `last` is `lastCompactionPoint`. If there's no compaction, `last` would be `undefined`. Then `last - keepTail` would be `NaN` (or throw an error). However, the code uses `Math.max(0, last - keepTail)`. If `last` is `undefined`, then `last - keepTail` is `NaN`, and `Math.max(0, NaN)` returns `NaN`. Then `tailStart` becomes `NaN`, and `slice(NaN, currentTurn)` would return an empty array? That would be wrong.
   - **Verdict**: **WORSE**. This is a bug. The code should handle the case when `last` is `undefined` (i.e., no compaction has occurred). The expected behavior is to return the full transcript from turn 0 to `currentTurn`. The current code would break.

2. **Question**: `keepTail=2` is hardcoded in the mini-bench call. The runner uses `keepTail` from RunOptions (also 2). Match achieved, but what if they diverge later? Should the harness read keepTail from runner results to stay in sync?
   - **Analysis**: The fix hardcodes `keepTail=2` in the mini-bench. However, the runner uses `keepTail` from `RunOptions`. If they diverge, the mini-bench and runner would have different behavior. The harness should read `keepTail` from the runner results to ensure consistency.
   - **Verdict**: **NEW_FLAW**. The fix should use the same `keepTail` value as the runner. Currently, it's hardcoded and might diverge.

### S18 (uniform cap) Fix
The fix applies the context-window cap (1200 chars) to all strategies.

3. **Question**: Now every strategy gets capped at 1200 chars. For `reactive`, the 5-section markdown recap is typically ~500-800 chars + tail ~400-600 chars → ~1000-1400 chars. Some recaps will be truncated. Is the right-aligned `simulateContextWindow` truncating from the WRONG end (the recap, not the older tail messages)?
   - **Analysis**: The `simulateContextWindow` function (not shown) is described as "right-aligned", meaning it keeps the last 1200 characters. For `reactive`, the recap is built from the entire history (including the tail) and then the tail is appended. The recap is built from the entire history (which might be long) and then the tail is appended. The truncation would remove the oldest parts, which are the recap's older parts and the tail's older parts. However, the recap is built from the entire history, so if the recap is truncated, it might remove the oldest parts of the recap (which are the oldest events) and keep the newest parts of the recap and the entire tail. This might be acceptable because the tail is the most recent. But note: the recap is built from the entire history, so if the recap is truncated, it might remove the oldest events that are not in the tail. This could be a problem because the recap is supposed to summarize the entire history. However, the tail is kept verbatim. So the truncation might remove the oldest events from the recap, but the tail (which includes the most recent events) is kept. This might be acceptable because the tail is the most recent and the recap is a summary. But the question is: is it truncating the recap (which is the summary) or the tail? Actually, the entire context (recap + tail) is being truncated. Since the recap is built from the entire history, it might be long. The truncation would remove the oldest parts of the entire context, which are the oldest parts of the recap and the oldest parts of the tail. However, the tail is the most recent, so the truncation would remove the oldest parts of the recap and the oldest parts of the tail? Actually, the tail is appended after the recap, so the entire context is: [recap (which is built from the entire history)] + [tail (the last `keepTail` turns)]. The truncation would remove the oldest parts of the entire context, which are the oldest parts of the recap. The tail is at the end and would be kept until the total length exceeds 1200. So the recap might be truncated, but the tail is kept as much as possible. This might be acceptable because the tail is the most recent and the recap is a summary. However, the recap might become incomplete. But note: the recap is built from the entire history, so if the recap is truncated, it might remove the oldest events that are not in the tail. This could be a problem because the recap is supposed to summarize the entire history. But the tail is kept verbatim, so the oldest events that are in the tail are kept. The events that are not in the tail (and are in the recap) might be truncated. This might be acceptable because the tail is the most recent and the recap is a summary. However, the truncation might remove important events from the recap. This is a trade-off. The question is: is it truncating the recap (which is the summary) or the tail? It truncates the entire context, so both the recap and the tail might be truncated. But the tail is at the end, so the truncation would remove the oldest parts of the recap and the oldest parts of the tail? Actually, the entire context is a string. The truncation removes the oldest characters (from the beginning). So the recap (which is at the beginning) would be truncated, and the tail (which is at the end) would be kept until the total length is 1200. So the recap might be truncated, but the tail is kept as much as possible. This might be acceptable because the tail is the most recent. However, the recap might become incomplete. But note: the recap is built from the entire history, so if the recap is truncated, it might remove the oldest events that are not in the tail. This could be a problem because the recap is supposed to summarize the entire history. But the tail is kept verbatim, so the oldest events that are in the tail are kept. The events that are not in the tail (and are in the recap) might be truncated. This might be acceptable because the tail is the most recent and the recap is a summary. However, the truncation might remove important events from the recap. This is a trade-off. The question is: is it truncating the recap (which is the summary) or the tail? It truncates the entire context, so both the recap and the tail might be truncated. But the tail is at the end, so the truncation would remove the oldest parts of the recap and the oldest parts of the tail? Actually, the entire context is a string. The truncation removes the oldest characters (from the beginning). So the recap (which is at the beginning) would be truncated, and the tail (which is at the end) would be kept until the total length is 1200. So the recap might be truncated, but the tail is kept as much as possible. This might be acceptable because the tail is the most recent. However, the recap might become incomplete. But note: the recap is built from the entire history, so if the recap is truncated, it might remove the oldest events that are not in the tail. This could be a problem because the recap is supposed to summarize the entire history. But the tail is kept verbatim, so the oldest events that are in the tail are kept. The events that are not in the tail (and are in the recap) might be truncated. This might be acceptable because the tail is the most recent and the recap is a summary. However, the truncation might remove important events from the recap. This is a trade-off. The question is: is it truncating the recap (which is the summary) or the tail? It truncates the entire context, so both the recap and the tail might be truncated. But the tail is at the end, so the truncation would remove the oldest parts of the recap and the oldest parts of the tail? Actually, the entire context is a string. The truncation removes the oldest characters (from the beginning). So the recap (which is at the beginning) would be truncated, and the tail (which is at the end) would be kept until the total length is 1200. So the recap might be truncated, but the tail is kept as much as possible. This might be acceptable because the tail is the most recent. However, the recap might become incomplete. But note: the recap is built from the entire history, so if the recap is truncated, it might remove the oldest events that are not in the tail. This could be a problem because the recap is supposed to summarize the entire history. But the tail is kept verbatim, so the oldest events that are in the tail are kept. The events that are not in the tail (and are in the recap) might be truncated. This might be acceptable because the tail is the most recent and the recap is a summary. However, the truncation might remove important events from the recap. This is a trade-off. The question is: is it truncating the recap (which is the summary) or the tail? It truncates the entire context, so both the recap and the tail might be truncated. But the tail is at the end, so the truncation would remove the oldest parts of the recap and the oldest parts of the tail? Actually, the entire context is a string. The truncation removes the oldest characters (from the beginning). So the recap (which is at the beginning) would be truncated, and the tail (which is at the end) would be kept until the total length is 1200. So the recap might be truncated, but the tail is kept as much as possible. This might be acceptable because the tail is the most recent. However, the recap might become incomplete. But note: the recap is built from the entire history, so if the recap is truncated, it might remove the oldest events that are not in the tail. This could be a problem because the recap is supposed to summarize the entire history. But the tail is kept verbatim, so the oldest events that are in the tail are kept. The events that are not in the tail (and are in the recap) might be truncated. This might be acceptable because the tail is the most recent and the recap is a summary. However, the truncation might remove important events from the recap. This is a trade-off. The question is: is it truncating the recap (which is the summary) or the tail? It truncates the entire context, so both the recap and the tail might be truncated. But the tail is at the end, so the truncation would remove the oldest parts of the recap and the oldest parts of the tail? Actually, the entire context is a string. The truncation removes the oldest characters (from the beginning). So the recap (which is at the beginning) would be truncated, and the tail (which is at the end) would be kept until the total length is 1200. So the recap might be truncated, but the tail is kept as much as possible. This might be acceptable because the tail is the most recent. However, the recap might become incomplete. But note: the recap is built from the entire history, so if the recap is truncated, it might remove the oldest events that are not in the tail. This could be a problem because the recap is supposed to summarize the entire history. But the tail is kept verbatim, so the oldest events that are in the tail are kept. The events that are not in the tail (and are in the recap) might be truncated. This might be acceptable because the tail is the most recent and the recap is a summary. However, the truncation might remove important events from the recap. This is a trade-off. The question is: is it truncating the recap (which is the summary) or the tail? It truncates the entire context, so both the recap and the tail might be truncated. But the tail is at the end, so the truncation would remove the oldest parts of the recap and the oldest parts of the tail? Actually, the entire context is a string. The truncation removes the oldest characters (from the beginning). So the recap (which is at the beginning) would be truncated, and the tail (which is at the end) would be kept until the total length is 1200. So the recap might be truncated, but the tail is kept as much as possible. This might be acceptable because the tail is the most recent. However, the recap might become incomplete. But note: the recap is built from the entire history, so if the recap is truncated, it might remove the oldest events that are not in the tail. This could be a problem because the recap is supposed to summarize the entire history. But the tail is kept verbatim, so the oldest events that are in the tail are kept. The events that are not in the tail (and are in the recap) might be truncated. This might be acceptable because the tail is the most recent and the recap is a summary. However, the truncation might remove important events from the recap. This is a trade-off. The question is: is it truncating the recap (which is the summary) or the tail? It truncates the entire context, so both the recap and the tail might be truncated. But the tail is at the end, so the truncation would
```

## opencode — infra-error (180042ms)

*Infra error*: exit null:        "question": "What is the seafood market reservation confirmation number?",
       "expectedKeywords": ["RES-J-A4K7"]
     }
   ],
-  "compactionPoints": [8]
+  "compactionPoints": [10]
 }
[0m


## codex — ok (178278ms)

```
**Findings**

- `REMAINS` `S13` systemic mismatch: the real runner still reconstructs context differently from the mini-bench helper. In [runner.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/src/runner.ts:163), `tailStart` is still `max(lastCompactionPoint, currentTurn - keepTail)`, so at `afterTurn=19`, `last=10`, `keepTail=2`, the runner tail starts at turn `17`, not `8`. It also still gives uncapped full transcript to non-compacted paths in [runner.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/src/runner.ts:181). R4 fixed the helper, not the benchmark’s primary evaluator.

- `REMAINS` `S2` measurement design flaw: the refactored fixture still probes facts that live in the preserved tail, so `reactive-vercel` can pass without its recap doing useful work. The probe is at turn `19` with compaction at `10` in [F-EN-TH-01-tool-heavy.fixture.json](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/src/fixtures/F-EN-TH-01-tool-heavy.fixture.json:28) and [F-EN-TH-01-tool-heavy.fixture.json](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/src/fixtures/F-EN-TH-01-tool-heavy.fixture.json:46); the asked-for IDs are still present in turns 11/12 and 17/18 at [F-EN-TH-01-tool-heavy.fixture.json](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/src/fixtures/F-EN-TH-01-tool-heavy.fixture.json:16) and [F-EN-TH-01-tool-heavy.fixture.json](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/src/fixtures/F-EN-TH-01-tool-heavy.fixture.json:22).

- `WORSE` `S18/S17`: the uniform 1200-char cap is now applied after concatenating `recap + tail`, and the truncation is right-aligned in [mini-bench-judge.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/scripts/mini-bench-judge.ts:98) and [mini-bench-judge.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/scripts/mini-bench-judge.ts:215). On this fixture, that can discard the recap entirely and keep only the latest tail. The unfairness moved, not disappeared.

- `REMAINS` `S15`: `parseInlineBlocks` still trims text blocks in [runner.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/src/runner.ts:448). That destroys leading/trailing whitespace and blank-line-sensitive formatting, which matters if fixtures start carrying markdown/code or spacing-dependent tool output.

- `NEW_FLAW` `S19`: `simulateContextWindow()` claims to start at a turn boundary, but it only searches for the next newline in [mini-bench-judge.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/scripts/mini-bench-judge.ts:100). Since turns themselves contain internal newlines, truncation can start mid-message, not at a real message boundary.

- `NEW_FLAW` `S20`: `parseInlineBlocks()` assigns `tool_result.toolCallId` from a per-message local counter in [runner.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/src/runner.ts:455) and [runner.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/src/runner.ts:484). With separate `role:"tool"` turns, this only works accidentally for one-call cases; multiple tool calls/results cannot be faithfully linked.

**Q1-Q9**

1. `S12`: full-transcript fallback still works when `last === undefined`. The guarded branch in [mini-bench-judge.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/scripts/mini-bench-judge.ts:136) requires `last !== undefined`; otherwise it falls back to `slice(0, currentTurn)` at [mini-bench-judge.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/scripts/mini-bench-judge.ts:150). `FIXED` for that helper path.

2. `S12` keepTail sync: `REMAINS`. Both callers hardcode `2` today in [mini-bench-judge.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/scripts/mini-bench-judge.ts:200) and [runner.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/src/runner.ts:549). There is no shared source of truth, so divergence is a future footgun.

3. `S18`: yes, the cap can truncate the wrong end. Because truncation is applied to the whole `recap + tail` string, the recap sits on the left and is the first thing dropped.

4. `S18`: yes, unfairness moved. `off` keeps the last 1200 chars of raw transcript; `reactive` keeps the last 1200 chars of `recap + tail`, so it can lose recap content while still paying recap overhead.

5. `S2` multiline thinking audit: `REMAINS` as an unproven area. The new fixture does not actually contain a true multi-line `[thinking]` block; it has single-line `[thinking]` followed by plain text, e.g. [fixture line 19](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/benchmarks/src/fixtures/F-EN-TH-01-tool-heavy.fixture.json:19). So the parser’s multi-line-thinking behavior is still not validated by this fixture.

6. `S2` probe design: `REMAINS`, and this is still fundamental. The tested IDs are in the preserved tail window, so the probe does not force recap-dependent recall.

7. `S13`: `REMAINS`. S18 did not unify the real evaluator. `runner.ts` still has no context-window cap and still uses different tail logic.

8. `S15`: `REMAINS`. Real-world impact is moderate now, high later: harmless for short prose, but it will corrupt markdown/code/spacing-sensitive fixtures.

9. `S17`: `WORSE`. The over-truncation mechanism is unchanged and now hits all strategies, including the compacted ones.

**Per-flaw Status**

- `S12`: `FIXED` in `mini-bench-judge.ts`, but only locally.
- `S18`: `REMAINS` functionally, with a new fairness inversion.
- `S2`: `REMAINS`.
- `S13`: `REMAINS`.
- `S15`: `REMAINS`.
- `S17`: `WORSE`.
- `S19`: `NEW_FLAW`.
- `S20`: `NEW_FLAW`.

**Final Verdict**

`STILL_PRELIMINARY`

R4 improves one real bug (`S12` in the helper) and makes the fixture shape more realistic, but the benchmark’s primary evaluator still disagrees with the helper, the new fixture still leaks the target facts through tail, and the uniform cap now risks deleting the very recap the compacted strategies are supposed to be judged on.
```

## gemini — ok (52130ms)

```
Topic: Adversarial Code-Review — Phase 1.3 R4 fixes
Strategic Intent: Providing final verdict on R4 fixes for naia-agent benchmarks.
Summary: I have completed the adversarial review of the R4 fixes. While S2 (fixture refactor) and S18 (uniform cap in the judge harness) are improved, the review has identified a catastrophic regression in `runner.ts` (S20) where the tail logic now deletes the "middle" of the transcript for probes far after compaction. Additionally, the discrepancy between the judge harness and the runner (S13) has widened, and a new flaw in parsing multi-line blocks (S19) was found. The measurement design (Question 6) remains flawed as it does not test compaction recall.

## Final Verdict: **WORSE_THAN_R3**

R4 successfully refactored the fixture (S2) and applied a uniform cap to the judge harness (S18), but it introduced a catastrophic regression in the core benchmark runner and failed to unify the logic between the harness and the runner.

### 1. S12 / S20 (CRITICAL REGRESSION): Broken Tail Logic in `runner.ts`

While `mini-bench-judge.ts` was fixed correctly (`tailStart = last - keepTail`), the core `runner.ts` received a broken implementation that deletes most of the history for late probes.

**File:** `packages/benchmarks/src/runner.ts` (Line 167)
```ts
const tailStart =
    lastCompactionPoint !== undefined
        ? Math.max(lastCompactionPoint, currentTurn - keepTail)
        : 0;
```
If `lastCompactionPoint` is turn 10 and `currentTurn` is 19 (as in the new fixture), `tailStart` becomes `max(10, 17) = 17`. 
- **The Result**: The "visible context" for the probe at turn 19 includes the recap (turns 0-8) and the tail (turns 17-18). 
- **The Missing Middle**: Turns **9, 10, 11, 12, 13, 14, 15, 16** are completely deleted.
- **Impact**: Fact-recall for Turn 11 (hotel ID) will fail deterministically in the runner even if the strategy is working perfectly. This makes the deterministic report useless for identifying "Missing Middle" bugs, as it now *creates* them.

### 2. S13 (REMAINS): Logic Mismatch Widened

The discrepancy between how the LLM judge sees context and how the deterministic runner evaluates it has grown:
- **Context Cap**: `mini-bench-judge.ts` now applies a 1200-char cap (S18), but `runner.ts` still has **no cap**.
- **Tail Calculation**: Harness uses `last - keepTail`; Runner uses `max(last, current - keepTail)`.
- **Outcome**: A strategy might "Pass" fact-recall in the runner (no cap) but "Fail" the LLM judge (capped/truncated), or vice versa due to the different tail slices.

### 3. S19 (NEW): Fragile Multi-line Parsing

The logic for parsing inline markers in `runner.ts` assumes each marker is a single line and that thinking blocks are not multi-line.

**File:** `packages/benchmarks/src/runner.ts` (Line 318)
```ts
for (const line of lines) {
    const thinkMatch = /^\[thinking\]\s*(.*)$/.exec(line);
    if (thinkMatch) {
        flushText();
        blocks.push({ type: "thinking", thinking: thinkMatch[1] ?? "" });
        continue;
    }
    // ...
    textBuf.push(line);
}
```
If a fixture contains a multi-line thinking block, only the first line is captured as `thinking`. The subsequent lines are treated as regular `text` blocks. While the current fixture is "flat," this is a systemic fragility.

### 4. Measurement Design (Question 6): Flaw Remains

The `F-EN-TH-01-tool-heavy` fixture still fails to test **compaction recall**.
- Compaction point is turn 10.
- Probes are at turn 19.
- Target facts (hotel ID at 11/12, seafood ID at 17/18) are all in the **post-compaction range**.
- Even if the summarizer is "broken" and produces an empty recap, the strategy will pass because the IDs are in the verbatim tail. To stress-test compaction, probes must ask about facts from turns 1-8 that are *not* repeated in the tail.

### 5. Review Question Answers

- **Q3 (Truncation end)**: `simulateContextWindow` keeps the tail. For `reactive`, this truncates the Recap header and potential summary content first. While "fair" (everyone is capped), it punishes strategies that produce high-value summaries at the head if the tail is also long.
- **Q5 (Multi-line)**: Confirmed broken (S19).
- **Q7 (S13 cap)**: Confirmed `runner.ts` still has no cap.

### Recommendations for R5
1. Sync `runner.ts`'s `tailStart` logic to match the correct `last - keepTail` formula.
2. Apply `simulateContextWindow` (with a shared constant) to `runner.ts` to unify the visible context.
3. Fix `parseInlineBlocks` to handle multi-line `[thinking]` blocks.
4. Add a probe to the fixture that asks for a fact from Turn 1-5 (e.g., "What was the initial weather forecast?") to actually test the summary recall.
```
