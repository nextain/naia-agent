# Cross-Review — 2026-05-21T04-40-08

**Prompt size**: 7899 chars
**AIs queried**: 4
**Valid responses**: 2/4

## glm — infra-error (0ms)

*Infra error*: GLM_API_KEY not set

## opencode — ok (91008ms)

```
Let me read the actual source files directly to see the exact code.
Now let me read the test files:
Now let me check the runtime package index exports and verify the `ai` dependency lives in runtime but not core:
Now let me verify tests pass:
All 32 new tests pass. Here is my review:

---

## Verdict: **APPROVE**

The change is well-scoped, correctly layered, and the double-compaction guard is watertight. Specific answers to the six review questions:

### 1. Layering — clean

`packages/core` depends only on `@nextain/agent-types` (`LLMMessage`, `LLMContentBlock`). The `prepareCompact` callback signature at `agent.ts:125-127` uses `readonly LLMMessage[]` and `LLMMessage[] | undefined` — zero SDK types. The `ai` package lives exclusively in `packages/runtime/package.json`. Confirmed: `core/package.json` has no `ai` dependency.

### 2. Double-compaction guard — correct

The guard is a simple early-return at `agent.ts:697-699`:

```ts
if (this.#strategy === "reactive" && this.#prepareCompact) {
  return this.#runPrepareCompact();
}
```

This returns **before** the `memory.compact()` path (lines 701-747). The `compactedThisTurn` flag at `agent.ts:300/319-323` prevents re-entry within the same turn's tool-hop loop. The `exportHandoff()` path (lines 796-836) is a separate feature that calls `memory.compact()` directly — it's gated by `handoffThreshold` and `#compactedThisSession`, not the per-turn guard. Tests AVC-01/02/03/07 correctly isolate this via `handoffThreshold: 0`.

### 3. Adapter lossiness — acceptable, one gap to note

- `redacted_thinking`: intentionally dropped (no SDK `reasoning` equivalent). Correct.
- `image` with `source.type === "url"`: **silently dropped**. `llmBlockToUserPart` (`vercel-prepare-step.ts:276-286`) only handles `source.data` (base64). If a history contains an image block with `{ type: "image", source: { type: "url", url: "..." } }`, it's lost. This is unlikely to cause issues in practice (LLM histories typically carry base64 images), but worth a brief JSDoc note. **Non-blocking — not a correctness bug, just a documented limitation.**

### 4. Threshold semantics — correct design

`createLLMMessagePrepareCompact` defaults `compactAfterTokens: 0` (`vercel-prepare-step.ts:394`), meaning the factory's inner threshold is always exceeded when called. The Agent's `contextBudget` at `agent.ts:691` is the sole trigger. This is the right design — double-gating would be confusing and the host can opt in via an explicit `compactAfterTokens` if desired (tested in ADP-09).

### 5. Error swallowing — correct (no fallback)

When `prepareCompact` throws, `#runPrepareCompact` logs + returns `undefined` (`agent.ts:661-665`). No fallback to `memory.compact()` because the host explicitly opted out of the in-house path. Falling back would violate the "exactly one path" contract and could surprise a host that disabled `memory.compact()` intentionally. AVC-07 validates this: turn survives, no `memory.compact()` call.

### 6. Surgical changes — clean

Every touched line traces directly to "wire Vercel prune into Agent":
- `agent.ts`: `AgentOptions.prepareCompact` + `#prepareCompact` field + `#runPrepareCompact` method + 3-line branch in `#maybeCompact` — all additive, no refactoring of existing paths.
- `vercel-prepare-step.ts`: adapter functions + factory — Phase 1.2 scope.
- `naia-agent.ts`: `--reactive-vercel` flag + spread wiring at two Agent construction sites — symmetric.
- Test files: targeted, named test IDs.

No incidental cleanup, no style changes, no "while I'm here" edits.

### Minor observations (non-blocking)

- **`llmMessageToModelMessage` string-content user/assistant** (`vercel-prepare-step.ts:253-254`): uses `as ModelMessage` cast for string content. This is fine — the SDK accepts `string | Part[]` for content — but worth noting the cast is doing type-system work.
- **`modelMessageToLLMMessage` system-role coercion** (`vercel-prepare-step.ts:337-338`): system messages fold to `assistant`. This is documented. Fine for pruning output.
- **`naia-agent.ts:618-620`**: the condition `args.reactiveVercel && args.compactStrategy === "reactive"` is duplicated at line 1032-1034 for service mode. Correct and symmetric.

**32/32 tests pass. APPROVE.**
```

## codex — infra-error (180019ms)

*Infra error*: empty stdout

## gemini — ok (66780ms)

```
I have completed the cross-review for Slice 3-XR-Compact v2 / Phase 1.2 (#56) R1. Based on the provided excerpts and architecture description, here is my verdict and detailed feedback.

### Verdict: **APPROVE**

The implementation is clean, surgically applied, and respects the core architectural boundaries of the `naia-agent` project. It successfully integrates the Vercel AI SDK's `pruneMessages` capability while maintaining a lean core.

---

### Detailed Feedback

#### 1. Layering & Dependency Management
- **Observation**: The `packages/core` package remains SDK-agnostic. By injecting the functionality via the `prepareCompact` hook in `AgentOptions`, you've avoided a direct dependency on the `ai` SDK in the core logic.
- **Verification**: The signature `(history: readonly LLMMessage[]) => LLMMessage[] | undefined` correctly uses internal types from `@nextain/agent-types`.
- **Conclusion**: Architecture layering is perfectly preserved.

#### 2. Double-Compaction Guard Correctness
- **Logic Check**: In `packages/core/src/agent.ts`, the new branch in `#maybeCompact`:
  ```ts
  if (this.#strategy === "reactive" && this.#prepareCompact) {
    return this.#runPrepareCompact();
  }
  ```
  This effectively creates a mutually exclusive path. If the conditions are met, `#runPrepareCompact` is executed and its result (even if `undefined`) is returned, bypassing the legacy `this.#memory.compact()` call.
- **Handoff Isolation**: As noted in the prompt, `exportHandoff` calls `memory.compact()` directly. Since this is a separate terminal path (handoff), it does not trigger a "double compaction" within the same turn's generation loop. The test isolation via `handoffThreshold: 0` is a sound strategy for verification.

#### 3. Adapter Lossiness
- **Thinking Blocks**: The loss of `redacted_thinking` is a known trade-off. For models with native CoT (like Claude 3.7 Sonnet), this might slightly degrade performance over very long multi-turn sessions if the model loses its previous "train of thought." However, for general compaction, this is acceptable.
- **Image Data**: Preserving image data via `FilePart` (mapping to Vercel's `ImagePart`) is correct.
- **Pathological cases**: The check `if (pruned.length === 0) return undefined;` in the factory is crucial to prevent the Agent from accidentally wiping its entire history if the SDK pruner over-optimizes.

#### 4. Threshold Semantics
- **Decision**: Setting `compactAfterTokens: 0` in the factory is the right call. It allows the `Agent` class to own the "when to compact" logic (via `contextBudget`), while the Vercel helper owns the "how to prune" logic. This avoids conflicting threshold battles between the two layers.

#### 5. Error Handling & Fallback
- **Safety**: The `try...catch` block in `#runPrepareCompact` prevents a faulty compaction hook from crashing the entire agent loop. 
- **No-Fallback Policy**: The current implementation does **not** fall back to `memory.compact()` if `prepareCompact` fails or returns `undefined`. 
  - *Risk*: Possible context overflow on the next LLM call.
  - *Rationale*: Falling back might violate the "exactly one path" rule or lead to inconsistent history states if the hook partially succeeded but then threw. Given this is an opt-in feature, logging a warning and proceeding is a reasonable "fail-soft" approach.

#### 6. Surgical Changes (Karpathy 4 Principles)
- **Think Before Coding**: The design clearly anticipates the layering needs.
- **Simplicity**: No unnecessary abstractions; just a clean functional hook.
- **Surgical**: The `VPS-R2-05` fix for `toolCalls: "none"` is a justified "drive-by" fix as it ensures the SDK integration works as intended with valid parameters.

### Minor Recommendation
In `#runPrepareCompact`, if `dropped === 0`, the agent still emits a `compaction` event. While not a bug, it might be slightly noisy for users monitoring events. However, for debugging the "reactive" loop, seeing that a check was performed (even if 0 messages were dropped) can be beneficial.

```ts
// packages/core/src/agent.ts
if (!pruned || pruned.length === 0) return undefined; 
// If pruned.length === before, dropped is 0. 
// Returning the event is fine, but consider if it's worth a 'trace' log.
```

Overall, the change is ready for merge.
```
