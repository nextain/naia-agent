# Cross-Review — 2026-05-21T04-51-21

**Prompt size**: 4800 chars
**AIs queried**: 4
**Valid responses**: 3/4

## glm — ok (65083ms)

```
### Verdict: **APPROVE**

### Rationale:
All three `codex` issues from R1 have been resolved with surgical, targeted fixes that align with the project's TypeScript ESM strict and F09 cleanroom-only-dep constraints. The changes maintain layering integrity (core untouched), pass all tests (37/37 phase-1.2 + 470/471 runtime), and address edge cases robustly. Below is a breakdown per review question:

---

### 1. **codex #1 fix: No-op detection**  
**✅ Correct.** The `pruned.length >= history.length && afterChars >= beforeChars` condition correctly identifies no-ops:  
- **Edge case handling**: If pruning replaces content with equal-length text (e.g., `{"text": "A"}` → `{"text": "B"}`), the `>=` comparison still triggers `undefined` because `afterChars >= beforeChars` holds. This is defensive and aligns with the goal of rejecting non-shrinking operations.  
- **Boundary logic**: `>=` is appropriate since it catches cases where message count *or* char count fails to shrink. Only when both metrics are non-reduced is it a true no-op.  

---

### 2. **codex #2 fix: URL-backed images**  
**✅ Correct.** The `new URL(...)` + `instanceof URL` pattern is valid:  
- **SDK compatibility**: `FilePart` explicitly accepts `DataContent | URL`, so the type check is sound.  
- **Serialization safety**: Malformed URLs will throw at runtime, which is acceptable since input validation is the caller's responsibility (consistent with existing patterns like `packages/providers/src/vercel-client.ts`). No serialization gotchas exist for valid URLs.  

---

### 3. **codex #3 fix: tool_result.isError**  
**✅ Correct.** Empty-error handling is consistent:  
- **Behavior**: `{ type: "error-text", value: "" }` round-trips to `isError: true, content: ""` is intentional. Normalizing empty errors would lose state (e.g., distinguishing "no error" vs. "empty error"), so preserving both fields is correct.  

---

### 4. **Defense in depth: No-op gate**  
**✅ Sufficient.** The char-shrinkage check is the right guardrail:  
- **Estimator vs. actual reduction**: The injected estimator predicts budget usage, but the no-op gate acts on *actual* reduction. This separation is intentional—budget overflows are handled by the agent's fallback logic, while the gate prevents false-success compaction events. No additional checks are needed.  

---

### 5. **Layering concerns**  
**✅ None introduced.** R2 changes are confined to `runtime` and:  
- Follow karpathy 4원칙 (surgical: only touch `createLLMMessagePrepareCompact`, `llmBlockToUserPart`, etc.).  
- Avoid core dependencies (F09 cleanroom).  
- Maintain backward compatibility (e.g., `FilePart` type safety).  

---

### Final Notes:
- **Test coverage**: New tests (ADP-08b, ADP-09b/c/d, AVC-08) cover all edge cases (no-op, URL images, error propagation).  
- **Performance**: Char-counting is O(n) and only runs post-prune, which is acceptable given the pruning operation's cost.  
- **Risk**: Low—changes are isolated and regression-tested.  

**Proceed with merge.**
```

## opencode — ok (86814ms)

```
Now let me read the full source files to verify the changes in context:
All 22 phase-1.2 tests pass (14 ADP + 8 AVC). Now let me verify the specific edge cases raised in the review questions:
Now let me check the Agent's `#maybeCompact` to verify the no-op → undefined path is handled correctly:
Now let me also check `new URL()` error handling — what happens if a malformed URL string reaches `llmBlockToUserPart`:
Now let me check the VercelClient pattern referenced for isError handling:
I've now reviewed all the code thoroughly. Here's my verdict:

---

## Verdict: **APPROVE**

All three codex R1 issues are correctly addressed. 22/22 phase-1.2 tests pass (14 ADP + 8 AVC). Specific answers to each review question:

### Q1 (no-op gate): Correct

The `>=` comparison on both `pruned.length` vs `history.length` AND `afterChars` vs `beforeChars` is the right boundary. A prune that replaces content with different but equal-length text is correctly treated as no-op — that's exactly what `pruneMessages` does (it strips blocks, it doesn't rewrite text). The AND condition means it only returns `undefined` when **neither** dimension shrunk. If message count decreased but char count stayed the same (or vice versa), the prune is accepted. This is the correct semantics.

### Q2 (URL images): Correct

`new URL(block.source.data)` will throw on malformed URLs — but that's the right behavior. The caller (`llmMessageToModelMessage`) is invoked from `createLLMMessagePrepareCompact` which wraps the entire pipeline in the inner `createVercelCompactionPrepareStep` that has try/catch around `pruneMessages`. More importantly, malformed URL data shouldn't exist in history — it came from a provider that parsed it successfully. `instanceof URL` + `.toString()` on the reverse path is the standard JS pattern.

### Q3 (empty error): Correct as-is

`{ type: "error-text", value: "" }` round-trips faithfully to `isError: true, content: ""`. This preserves the original semantics. Normalizing empty errors to non-errors would lose information. The existing VercelClient pattern at `vercel-client.ts:347` does the same mapping, confirming consistency.

### Q4 (defense in depth): Sufficient for Phase 1.2

The char-count + message-count gate catches the practical failure mode. A budget-aware check would require injecting the estimator into the factory, adding coupling. The current Agent already gates on budget *before* calling `prepareCompact`, so a no-op rejection just means "pruning couldn't help" and the Agent proceeds without compaction (same as the in-house path's failure mode). Acceptable.

### Q5 (layering): Clean

All R2 changes are in `packages/runtime/src/compaction/vercel-prepare-step.ts` and its tests. Zero changes to `packages/core`. The `LLMMessage` type from `@nextain/agent-types` remains the only cross-package contract.

**Non-blocking observation**: `countContentChars` counts `image.source.data.length` which for URL images counts the URL string length, not the actual image size. This is fine for the no-op gate (URL images aren't pruned differently), but worth noting if someone later uses this function for budget estimation.
```

## codex — infra-error (180010ms)

*Infra error*: empty stdout

## gemini — ok (73796ms)

```
## Verdict: **APPROVE**

R2 addresses all 3 findings from the codex R1 review with surgical and idiomatic changes. The implementation remains architecturally sound, preserving the core's SDK-agnosticism while enhancing the robustness and fidelity of the reactive compaction strategy.

### Analysis of R2 Fixes

1.  **codex #1 (No-op Prune Rejection)**: ✅ **FIXED**.
    - The addition of `countContentChars` and the comparison gate (`pruned.length >= history.length && afterChars >= beforeChars`) correctly identifies ineffective pruning.
    - Returning `undefined` for no-ops prevents the Agent from incorrectly marking the turn as compacted and suppressing necessary fallbacks. The char-count proxy is sufficient and aligns with the default estimator's logic.

2.  **codex #2 (URL Image Round-trip)**: ✅ **FIXED**.
    - The logic now correctly branches on `source.type === "url"` and utilizes the SDK's `FilePart` ability to accept a `URL` object.
    - The reverse adapter's `instanceof URL` check is clean and prevents the silent base64 mislabeling identified in R1.

3.  **codex #3 (Tool Result Error State)**: ✅ **FIXED**.
    - Mapping `isError: true` to the Vercel `error-text` part type is a clever use of existing SDK primitives.
    - The reverse adapter correctly restores the `isError` bit, ensuring model-visible conversation state is preserved across the compaction boundary.

### Responses to Review Questions

1.  **No-op gate**: The `>=` boundary is correct. Since `pruneMessages` only removes or empties blocks, any result where neither length nor char-count decreased is functionally a no-op.
2.  **URL handling**: `instanceof URL` is the standard and correct way to handle this distinction. Malformed URLs would be caught by the existing adapter error boundaries.
3.  **Empty-error normalization**: Preserving the empty error is preferred for fidelity. Normalizing to something else would be an unnecessary side effect.
4.  **Defense in depth**: The char-shrinkage gate is sufficient for this reactive path.
5.  **Layering**: No new concerns; logic remains correctly encapsulated in `packages/runtime`.

The added tests (`ADP-08b`, `ADP-09b/c/d`, `AVC-08`) provide strong regression guards for these fixes. R2 is ready for merge.
```
