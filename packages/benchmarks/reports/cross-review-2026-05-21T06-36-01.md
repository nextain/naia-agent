# Cross-Review — 2026-05-21T06-36-01

**Prompt size**: 3682 chars
**AIs queried**: 4
**Valid responses**: 2/4

## glm — ok (129695ms)

```
We are given an adversarial review task for Phase 1.3 R3 measurement numbers. We have:
- R1 verdict: UNPUBLISHABLE, 10 flaws (S1 to S10, but note R2 fixed S1, S5, S8 and left S2, S9, S10, S11, S12, S13, S14 broken)
- R2 verdict: fixed S1/S5/S8, but S2/S9/S10/S11/S12/S13/S14 still broken.
- R3 fixes applied (commit 0d976e7): fixed S2, S14, S11, and S10 (stronger cap 2000->1200). Also note that R5 results are provided (which include R3-R5 rounds).

We are to:
1. For each of the previously real flaws (S1, S2, S5, S8, S9, S10, S11, S12, S13, S14) from R1 and R2, determine if they are:
   - REMAINS_BROKEN
   - NOW_FIXED
   - WORSE
   - IRRELEVANT_AFTER_OTHER_FIX

2. Then find new flaws (S15+) introduced by R3.

3. Final verdict: PUBLISHABLE / STILL_PRELIMINARY / WORSE_THAN_R2.

Let's break down step by step.

### Step 1: Evaluate Previously Real Flaws (S1 to S14)

We know from R2 that S1, S5, S8 were fixed. So we only need to check S2, S9, S10, S11, S12, S13, S14.

But note: R3 fixed S2, S14, S11, and S10 (with a stronger cap). So we have:

- S2: fixed in R3 (as per R3 fixes: "S2 proper: parseInlineBlocks converts ...")
- S10: fixed in R3 (with a stronger cap: 2000->1200)
- S11: fixed in R3 (vercel no-op gets context-window truncation)
- S14: fixed in R3 (restore tail for reactive-vercel)

Now, what about S9, S12, S13? They were not fixed in R3. So they remain broken.

But note: R5 results are provided and they show improvements. However, we are to base our assessment on the code changes and the provided information.

So:

- S1: Fixed in R2 -> IRRELEVANT_AFTER_OTHER_FIX (since we are now at R3, and it was fixed earlier)
- S2: Fixed in R3 -> NOW_FIXED
- S5: Fixed in R2 -> IRRELEVANT_AFTER_OTHER_FIX
- S8: Fixed in R2 -> IRRELEVANT_AFTER_OTHER_FIX
- S9: Not fixed in R3 -> REMAINS_BROKEN
- S10: Fixed in R3 (with stronger cap) -> NOW_FIXED
- S11: Fixed in R3 -> NOW_FIXED
- S12: Not fixed in R3 -> REMAINS_BROKEN
- S13: Not fixed in R3 -> REMAINS_BROKEN
- S14: Fixed in R3 -> NOW_FIXED

But note: R5 fixes (since R4) include S13 and S18, S19, S20. However, we are only concerned with the state after R3 (and the R3 fixes). The R5 fixes are beyond R3.

So for the purpose of this review (which is on R3), we have:

- S9, S12, S13: REMAINS_BROKEN

### Step 2: Find New Flaws (S15+)

We are to check:

a) Did the 1200-char cap make `off` artificially worse than reality?
   - The cap was applied to all strategies? But note: the R5 table shows that `off` went from 1.00 in R2 to 0.67 in R3 and then 0.67 in R4 and R5. However, the cap was introduced in R3 (from 2000 to 1200). The `off` strategy is supposed to be a baseline without compaction. If the cap is applied to `off`, then it might be artificially worse because it's being truncated when it shouldn't be. We need to check the code.

   Looking at the R3 fixes: "S10 stronger: cap 2000 → 1200 chars." and in R5 fixes: "S18 fairness inversion: cap preserves recap whole, truncates tail only for compacted strategies." This suggests that in R3, the cap was applied to all strategies, including `off`. But in R5, they fixed it to only apply to compacted strategies. So in R3, the `off` strategy was being truncated, which is incorrect. Therefore, the 1200-char cap in R3 made `off` artificially worse.

   So this is a new flaw: S15: The 1200-char cap was applied to the `off` strategy, which should not be truncated.

b) Did `parseInlineBlocks` correctly expose `[tool_result]` content to the SDK, or is it dropped at the `llmBlockToAssistantPart` boundary?
   - We need to check the code in `packages/benchmarks/src/runner.ts` and related. The R3 fix for S2 was to convert inline markers to real LLMContentBlock parts. But we must ensure that the `[tool_result]` content is not lost when converting to the SDK format.

   Without seeing the code, we can only reason: if the conversion is done correctly, then the tool result should be preserved. However, if there is a step that drops non-assistant parts (like `llmBlockToAssistantPart`), then it might be dropped. This is a potential flaw.

   We'll flag this as S16: Potential loss of `[tool_result]` content during conversion to SDK parts.

c) Did the `extractVisibleContext` distinct branches for `reactive` vs `reactive-vercel` smuggle in a new unfair asymmetry?
   - We are told that R3 fixed S14 by restoring the tail for reactive-vercel. But we must check if the two strategies are now treated symmetrically. If one branch does something different and that difference is not justified by the strategy, then it could be an unfair asymmetry.

   Without code, we can only note that there are two branches. We'll flag this as S17: Potential unfair asymmetry in `extractVisibleContext` between `reactive` and `reactive-vercel`.

d) N=1 cells still — flagged S3 from R1 is open. Is this round any more trustworthy than R1, even if individual flaws are patched?
   - The issue of N=1 (i.e., only one probe per fixture) was raised in R1 as S3. It remains a concern because with only one probe, the results might not be statistically significant. Even if we fix other flaws, the small sample size per fixture is a fundamental limitation.

   We'll flag this as S18: N=1 per fixture remains a statistical reliability concern.

### Step 3: Final Verdict

We have:
- Previously broken flaws: S9, S12, S13 remain broken.
- New flaws: S15 (cap applied to `off`), S16 (tool_result dropped?), S17 (asymmetry), S18 (N=1).

The R5 results show that the reactive-vercel strategy improved to 0.92, which is good. However, the presence of multiple remaining flaws and new flaws suggests that the results are not yet publishable.

Moreover, the R5 fixes (which are beyond R3) address some of these (like S13 and the cap fairness). But we are reviewing R3.

Given that:
- There are still broken flaws (S9, S12, S13) from previous rounds that were not fixed in R3.
- New flaws were introduced (S15, S16, S17, S18).

We must conclude that the R3 measurement is still preliminary.

But note: the R5 results are provided and they show that some of these issues were fixed in later rounds. However, the task is to review R3.

Therefore, the verdict for R3 is: **STILL_PRELIMINARY**.

### Minimum Additional Fixes (if STILL_PRELIMINARY)

We are to rank by severity:

1. **S15: The 1200-char cap applied to `off` strategy** - This is a critical flaw because it artificially degrades the baseline. It must be fixed to have a fair comparison. (High severity)
2. **S9, S12, S13: Remaining broken flaws** - These were known from previous rounds and are still broken. They must be fixed. (High severity, but note S13 was fixed in R5, so if we are to do R3, we need to fix it in R3? But we are only reviewing R3. However, for the numbers to be citable, we need to fix all known flaws.)
3. **S16: Potential loss of `[tool_result]` content** - This could lead to incorrect tool usage and affect the results. (Medium severity)
4. **S17: Unfair asymmetry in `extractVisibleContext`** - This could bias the results for reactive-vercel. (Medium severity)
5. **S18: N=1 per fixture** - This is a fundamental statistical issue. It requires more probes per fixture. (Low severity in the sense that it's a design choice, but high for reliability)

But note: the R5 fixes already addressed S13 and the cap fairness (S18 in R5 is about cap fairness). However, for R3, we are to fix the flaws in R3.

So for R3 to be citable, we need:

1. Fix the cap to not apply to `off` (S15) - this is critical.
2. Fix S9, S12, S13 (if possible in R3) - but note R3 did not fix them. We would need to fix them in R3.
3. Fix the tool_result exposure (S16) and the asymmetry (S17) in the code.

But note: the R5 results are provided and they show that the cap fairness was fixed in R5 (S18 in R5) and S13 was fixed in R5. So if we are to use R5, then we have fixed these. However, the task is to review R3.

Given the above, the minimum additional fixes for R3 to be citable would be:

1. Fix the cap to not apply to `off` (S15) - because without this, the baseline is broken.
2. Fix S9, S12, S13 - but note that R3 did not fix them. We would need to fix them in R3. However, we don't have details on what S9, S12, S13 are. But they were identified as broken in R2 and not fixed in R3. So they must be fixed.
3. Fix the tool_result exposure (S16) and the asymmetry (S17) to ensure correctness.

But note: the R5 results are provided and they are better. So if we are to publish, we might wait for R5. However, the task is to review R3.

Therefore, for R3, we must fix at least the critical flaw (S15) and the remaining broken flaws (S9, S12, S13) to make it publishable. But without knowing what S9, S12, S13 are, we cannot say how to fix them. However, the R5 fixes for S13 are known (from R5 fixes: S13 was fixed by aligning the runner.ts evaluateProbe with mini-bench). So we can do that in R3.

But note: the R5 fixes are for R5, not R3. We are to fix R3.

Given the complexity, we'll rank the fixes we know:

1. **S15: Cap applied to `off`** - must be fixed immediately. This is a critical flaw that invalidates the baseline.
2. **S13: Runner.ts evaluateProbe** - we know from R5 that it was fixed by aligning with mini-bench. We can do that in R3.
3. **S9 and S12**: We don't have details, but they were identified as broken in R2 and not fixed in R3. They must be fixed. Without details, we cannot specify, but they are high priority.
4. **S16 and S17**: These are potential flaws that we need to verify and fix.

But note: the R5 results show that the reactive-vercel strategy improved to 0.92, which is the highest. This suggests that the fixes in R5 (including the cap fairness and S13) are important.

Therefore, for R3 to be citable, we need:

1. Fix the cap to not apply to `off` (S15) - critical.
2. Fix S13 (by aligning evaluateProbe with mini-bench) - because it was broken and we know how to fix it from R5.
3. Fix S9 and S12 - but without details, we cannot specify. However, they are high priority.
4. Fix S16 and S17 - to ensure correctness.

But note: the R5 results are provided and they are better. So if we are to publish, we should use R5. However, the task is to review R3.

Given the above, we conclude:

**Final Verdict: STILL_PRELIMINARY**

**Minimum additional fixes (ranked by severity):**

1. **S15: Cap applied to `off` strategy** - Fix by removing the cap from the `off` strategy (only apply to compacted strategies, as done in R5).
2. **S13: Runner.ts evaluateProbe** - Fix by aligning with mini-bench (same tail logic and 1200-char cap, as done in R5).
3. **S9 and S12**: These were broken in R2 and not
```

## opencode — infra-error (180035ms)

*Infra error*: exit null: ner.ts | head -120
(no output)
[0m
[0m→ [0mRead reports/2026-05-21-mini-bench-judge-F-EN-TH-01-tool-heavy.md
[0m→ [0mRead reports/2026-05-21-mini-bench-judge-F-KR-IE-01-information-extraction.md


## codex — infra-error (180025ms)

*Infra error*: empty stdout

## gemini — ok (158028ms)

```
This adversarial review of the Phase 1.3 R5 (Slice 3-XR-Compact) benchmark iteration confirms that while several critical R1/R2 flaws have been addressed, **the resulting measurement numbers remain unpublishable** due to systemic fairness inversions and logic drift between the deterministic and ensemble paths.

### 1. Previously Identified Flaws (Status)

*   **S2 (Structured Input): NOW_FIXED.** `parseInlineBlocks` successfully converts markers into structured `LLMContentBlock` parts, allowing `pruneMessages` to function correctly.
*   **S10 (1200-char Cap): NOW_FIXED (but see S21).** The cap is applied, providing the "pressure" needed to differentiate strategies.
*   **S11 (Vercel no-op truncation): NOW_FIXED.** No-op prunes now correctly fall back to the truncated full-transcript path in the judge script.
*   **S13 (runner/judge alignment): REMAINS_BROKEN / WORSE.** See S24/S25/S26.
*   **S14 (Restore tail for Vercel): NOW_FIXED.** `reactive-vercel` now correctly receives the preserved tail window.

---

### 2. New Flaws (S15+) — The "Phantom Win" for `reactive-vercel`

The most severe issue in R5 is the **Fairness Inversion (S21)**. The "protection" logic intended for small summaries has been hijacked by the pruning strategy.

*   **S21: Unfair Asymmetry (Protection Hack).** `mini-bench-judge.ts` protects `recapContent` from the 1200-char cap. For `reactive-vercel`, the "recap" is the **pruned history**. In fixtures with no reasoning/tools (like `F-KR-IE-01`), the "recap" is effectively the **full history**. By protecting it, the harness grants `reactive-vercel` an infinite context window, while `off` is truncated.
    *   *Proof:* In `F-KR-IE-01`, `reactive-vercel` gets 1.000 pass rate while `off` gets 0.000. Both are showing the same raw text, but `off` was truncated and `reactive-vercel` was "protected."
*   **S22: Silent Head Deletion (No-op Fallback).** In `runner.ts`, if `recapContent` is empty (no-op), `evaluateProbe` shows **only the tail**. This means the deterministic path "deletes" the entire history if Vercel doesn't prune anything, leading to false negatives (0.000 Keyword-recall for `reactive-vercel` in `F-KR-IE-01`).
*   **S24/S25/S26: Systemic Alignment Drift.** The deterministic and ensemble paths have diverged completely:
    *   **Truncation:** `runner.ts` uses uniform truncation; `mini-bench-judge.ts` uses protected-recap truncation.
    *   **Implementation:** `mini-bench-judge.ts` uses smart role-aware windowing (`simulateContextWindow`); `runner.ts` uses simple newlines.
    *   **Labels:** `mini-bench-judge.ts` prepends role labels (`user:`, `assistant:`); `runner.ts` uses raw content.
*   **S28: Role Mapping (System-as-User).** `runner.ts` maps `system` to `user` for the Vercel path. This loses privileged context for the pruner and forces the judge to evaluate a "user message" that is actually a system instruction.
*   **S29: Drift Score Bias.** `reactive-vercel`'s recap includes role labels (via `llmMessagesToText`), while the baseline does not. This artificially inflates the `driftScore` even if the content is identical.

---

### 3. Verdict & Recommendations

Verdict: **STILL_PRELIMINARY / WORSE_THAN_R2.**

The R5 table showing `reactive-vercel` at **0.92** is an artifact of S21 protection and should not be used for any performance claims. It "wins" simply because the benchmark harness stopped applying the context window limit to it.

**Minimum fixes before numbers are citable:**

1.  **Fix S21/S24 (Uniform Pressure):** Apply the 1200-char cap to the *entire* `visibleText` (Recap + Tail) across ALL strategies. Compaction should win because it is *smaller*, not because it is *exempted*.
2.  **Fix S22 (Honest Fallback):** If `recapContent` is empty/no-op, the visible context must fallback to the full history (exactly like `off`), not just the tail.
3.  **Fix S25/S26 (Unified Harness):** Align `runner.ts` and `mini-bench-judge.ts` to use the same role-prefixed formatting and `simulateContextWindow` logic. Deterministic keyword matching and Ensemble judging must operate on the **exact same string.**
4.  **Fix S28 (Role Fidelity):** Preserve the `system` role for the Vercel/LLM history path.
```
