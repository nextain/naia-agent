# Cross-Review — 2026-05-21T05-45-19

**Prompt size**: 7102 chars
**AIs queried**: 4
**Valid responses**: 2/4

## glm — ok (88323ms)

```
We are reviewing R3 fixes for the following issues: S2, S10, S11, S14, and also re-verdicting S12 and S13. We must also look for new bugs (S15+).

Let's break down each part:

### S2 (proper) — `runner.ts toLLMMessage()` + `parseInlineBlocks()`

The goal is to fix the issue where fixture markers (like `[thinking]`, `[tool_use]`, `[tool_result]`) were being treated as string content instead of being parsed into SDK blocks.

1. **Handling of `[tool_use]` and `[tool_result]` in the same turn**:
   - The code parses each line and creates blocks. For an assistant turn that has both `[tool_use]` and `[tool_result]`, it will create two blocks: one of type `tool_use` and one of type `tool_result`.
   - However, in `vercel-prepare-step.ts`, the function `llmBlockToAssistantPart` (which converts LLM blocks to assistant parts) only handles `text` and `thinking` blocks. It does not handle `tool_result` blocks. Therefore, the `tool_result` block is dropped.
   - The question: are the IDs in `[tool_result]` content exposed to `pruneMessages`? 
     - The `pruneMessages` function (in `vercel-prepare-step.ts`) is called on the assistant message that has been converted to parts. Since the `tool_result` block is dropped, the ID is not included. Therefore, the `pruneMessages` function does not see the tool result ID and cannot use it to prune the corresponding tool call. This is a problem because the tool result is part of the assistant's response and should be pruned when the tool call is pruned.

   **Conclusion**: This is a bug. The `tool_result` block should be handled appropriately. One way is to convert the `tool_result` block into a part that can be pruned. Alternatively, we might need to adjust the pruning logic to account for tool results. However, note that the SDK does not have a direct representation for tool results in the assistant's message. The tool results are typically returned as separate messages (from the tool role). But in the fixture, they are included in the assistant turn. This suggests that the fixture format might be inconsistent with the SDK. 

   **Verdict**: This is a bug that was not fixed by R3. It might be a new bug (S15) or a regression? Actually, it was present in R2 and R3 didn't fix it. But note: the R3 fix for S2 was about parsing the markers, but the handling of tool results in the assistant part is a separate issue. So we mark it as **NEW_BUG** (or perhaps it was already present and not fixed? The R2 review didn't catch it?).

   However, note the R2 review said: "S2 DOESNT_FIX_R1 (fixture markers were string-in-content, not SDK blocks)". The R3 fix addressed the parsing, but the subsequent handling of tool results in the assistant part is a new issue we are discovering now.

   **Action**: We must flag this as a new bug (S15) because it was not present in the previous reviews and is a consequence of the R3 fix (or rather, the R3 fix exposed the issue by correctly parsing the tool result, but then the downstream processing doesn't handle it).

2. **Non-JSON input for `[tool_use]`**:
   - The code falls back to `input = { raw: ... }` if JSON parsing fails.
   - The SDK expects the input to be a JSON-serializable object. The fallback of `{ raw: ... }` is an object, so it is serializable. However, it might not be the intended behavior. The tool call should have the parameters as an object. If the input is not JSON, then it's invalid. But the fixture might have non-JSON? We should check the fixtures.

   **Conclusion**: This is acceptable because it ensures the input is an object. However, it might hide errors. But the problem says: "Is that correct for the SDK's `tool-call` part schema?" — the schema expects an object, and `{ raw: ... }` is an object. So it's acceptable.

   **Verdict**: **FIXED** (or at least, the fix is acceptable).

3. **Multi-line `[thinking]` content**:
   - The regex only matches the same line. Multi-line thinking would be split into multiple blocks? Actually, the code processes line by line. If a thinking block spans multiple lines, then each line would be a separate block? But note: the regex is applied per line. So if the thinking content is on multiple lines, each line would be a separate thinking block? That is not ideal.

   **Example**:
   ```
   [thinking]
   This is line 1
   This is line 2
   ```

   This would be split into two thinking blocks: one for "This is line 1" and one for "This is line 2". But the thinking should be one block.

   **Conclusion**: This is a bug. The function should handle multi-line thinking.

   **Verdict**: **NEW_BUG** (S15).

### S14 — `extractVisibleContext()` reactive-vercel tail restore

The fix adds a tail when the strategy is "reactive-vercel" and there is a recap.

2. **At probe `afterTurn` equal to `lastCompactionPoint`**:
   - `slice(last, currentTurn)` becomes `slice(N, N)` which is empty. So the tail is empty.
   - The visible context becomes: `[reactive-vercel post-prune window]\n${recapContent}\n\n` (with a trailing newline).
   - This is cosmetic. However, it might be confusing to have a trailing newline. But it doesn't leak any signal because the content is empty.

   **Verdict**: **FIXED** (cosmetic only, no functional issue).

### S11 + S10 stronger

3. **1200-char cap**:
   - The cap is set to 1200 characters (about 300 tokens). For a 30-turn fixture of 3000 characters, it keeps the last 10-13 turns. This might be too aggressive because it loses early facts that are important for answering probe questions.

   **Question**: Is this the right "production cap" intuition?
   - The comment says: "tighter than R2's 2000 to honestly represent that a real provider would force-truncate the head of a ~30-turn Korean transcript long before the agent's budget hook fired."
   - However, in production, the context window is fixed and the agent would have to manage it. But in the benchmark, we are simulating a fixed context window. The issue is that 1200 might be too small for the fixtures we have, leading to loss of important early information.

   **Verdict**: This might be **PARTIAL** because it is a tighter cap, but it might be too aggressive for the fixtures. Alternatively, it could be **NEW_BUG** if it causes the benchmark to fail due to missing context.

4. **Uniform cap vs. fixture-relative**:
   - The cap is fixed at 1200 regardless of fixture length. A fixture-relative cap (e.g., 40% of the total) might be more honest because it scales with the fixture.

   **Verdict**: This is a design decision. The current fix is **FIXED** (as per the comment) but we might argue for a different approach. However, the requirement was to fix S10 (which was about the cap being too lenient at 2000). The new cap is 1200, which is fixed. So we mark it as **FIXED** for S10, but note that the uniform cap might be a separate issue (S15).

### Re-verdict on R2-flagged but R3-untouched

5. **S12**: "runner.ts driftScore at line 391 double-tails when reactive-vercel"
   - The code in `runner.ts` for driftScore (lines 393-402) in the reactive-vercel strategy:
     ```ts
     if (strategy === "reactive-vercel") {
       const tail = fixture.turns
         .slice(last, currentTurn)
         .map((t) => `${t.role}: ${t.content}`)
         .join("\n");
       visible = `[reactive-vercel post-prune window]\n${recapContent}\n\n${tail}`;
     }
     ```
   - The recapContent for reactive-vercel is the full pruned history (which already includes the tail from the last compaction point to the current turn). Then we append the tail again? That would be double.

   **Verification**:
   - In reactive-vercel, the `recapContent` is built by `buildRecap` which includes the turns from `lastCompactionPoint` to the current turn? Actually, let's look at the code for `buildRecap` (in `runner.ts`). The `buildRecap` function for reactive-vercel does:
     ```ts
     if (strategy === "reactive-vercel") {
       const tail = fixture.turns
         .slice(last, currentTurn)
         .map((t) => `${t.role}: ${t.content}`)
         .join("\n");
       return `[reactive-vercel post-prune window]\n${tail}`;
     }
     ```
     So the recapContent is the tail from `last` to `currentTurn`. Then in `driftScore`, we are appending the same tail again? That would be double.

   **Conclusion**: This is a bug. The recapContent already contains the tail, so appending it again duplicates it.

   **Verdict**: **DOESNT_FIX** (R3 did not fix S12).

6. **S13**: "deterministic vs ensemble context-window logic mismatch"
   - The deterministic path (in `evaluateProbe` in `runner.ts`) uses `lastCompactionPoint + keepTail` to build the visible context.
   - The ensemble path (in `mini-bench-judge.ts`) uses `last + simulateContextWindow`.
   - R3 changed the context window truncation to be applied in more cases (S11) and reduced the cap (S10). But the two paths are still different.

   **Question**: Did R3 fix this? 
   - The deterministic path: 
     - It builds the recap (which for reactive-vercel includes the tail) and then appends the tail again (if reactive-vercel) — which we just found is a bug (S12). 
     - Then, if truncation is needed (S11 condition), it truncates the entire visible context to 1200 chars.
   - The ensemble path:
     - It uses `extractVisibleContext` which for reactive-vercel builds the recap and then appends the tail (again, the same bug as S12). 
     - Then, if truncation is needed (same condition as S11), it truncates to 1200 chars.

   So both paths have the same bug (S12) and the same truncation (S10+S11). But the way they build the context is still different because the deterministic path uses `lastCompactionPoint` and the ensemble path uses `last` (which is the same as `lastCompactionPoint`?).

   Actually, in `mini-bench-judge.ts`, the `last` is set to `lastCompactionPoint` (from the deterministic run). So the two paths are building the context in the same way? 

   However, note that the deterministic path in `evaluateProbe` does:
   ```ts
   const visible = buildRecap(strategy, last, currentTurn, recapContent);
   if (strategy === "reactive-vercel") {
     // ... append tail (which is the same as in the recap for reactive-vercel? -> bug)
   }
   ```

   And the ensemble path in `mini-bench-judge.ts` does:
   ```ts
   const visible = extractVisibleContext(strategy, last, currentTurn, recapContent);
   ```

   And `extractVisibleContext` for reactive-vercel does the same as the deterministic path: it appends the tail. So both paths have the same bug (S12). But the truncation is applied in the same way (S11 condition and 1200 cap). 

   Therefore, the mismatch is still present because the deterministic path and ensemble path are both doing the same thing (with the same bug). But the issue was that they were different. Now they are the same? 

   **Conclusion**: The two paths are now aligned in the way they build the context (both have the S12 bug) and both apply truncation the same way. So the mismatch is fixed? But note: the S12 bug is still present. However, the S13 issue was about a mismatch between the two paths. Now they are the same, so the mismatch is gone? 

   **Verdict**: **FIXED** for S13? But note: the S12 bug is still there, but that's a separate issue. The S13 issue was about the mismatch, and now both paths are the same (with the same bug). So the mismatch is fixed.

   However, we must note that the S12 bug is still present and will cause both paths to have the same error. But that doesn't fix S12.

### NEW flaws (S15+)

We have already identified two new bugs from S2:
- S15a: `tool_result` blocks in assistant turns are dropped by `llmBlockToAssistantPart`, causing loss of tool result IDs and preventing proper pruning.
- S15b: Multi-line thinking blocks are split into multiple blocks.

Additionally, we have:
- S15c: The uniform 1200-char cap might be too aggressive for some fixtures (but this is a design decision, not necessarily a bug).
- S15d: The S12 bug (double tail in reactive-vercel) is still present and affects both deterministic and ensemble paths.

But note: the S12 bug was already
```

## opencode — infra-error (180040ms)

*Infra error*: exit null: har cap logic
[✓] S12 re-verdict — double-tail in driftScore for reactive-vercel
[ ] S13 re-verdict — deterministic vs ensemble context-window mismatch
[ ] S15+ — find NEW bugs
[ ] Final verdict
[0m


## codex — infra-error (180025ms)

*Infra error*: empty stdout

## gemini — ok (195911ms)

```
# Adversarial Code-Review — Phase 1.3 R3 Final Verdict: REGRESSED

While R3 addresses some surface issues (S14 tail restoration), it introduces critical regressions in judge fairness, significantly diverges deterministic and ensemble logic (S13), and leaves a fundamental logic bug in context construction (S12) that invalidates the comparison between strategies.

## 1. S2 Proper Fix: DOESNT_FIX / NEW_BUG
The implementation of `parseInlineBlocks` in `runner.ts` fails to solve the underlying SDK mismatch and introduces new parsing flaws:
- **Invalid SDK Message Structure:** `parseInlineBlocks` creates `tool_result` content blocks within an `assistant` role message. In both Anthropic and Vercel AI SDKs, `assistant` messages can only contain `text` and `tool_use` (tool-call) parts. `tool_result` must belong to a message with the `tool` role. By keeping them in the assistant turn, these blocks are likely to be dropped or rejected by the `pruneMessages` helper, defeating the purpose of the fixture.
- **Multi-line `[thinking]` Split:** The regex `/^\[thinking\]\s*(.*)$/` only captures the first line. If a fixture turn has multi-line thinking, subsequent lines are flushed as `text` blocks, which `pruneMessages` (using `reasoning='all'`) will NOT strip, leading to incorrect benchmark results.
- **Marker Rigidity:** The `^` anchor ensures that if a fixture author accidentally includes a leading space, the marker is ignored.

## 2. S10/S11 Stronger Truncation: DOESNT_FIX / REGRESSED
The 1200-character cap (S10) is applied incorrectly and creates an unfair comparison:
- **Unfair Advantage (S18):** In `mini-bench-judge.ts`, the cap is **only** applied to `off`, `anthropic-native`, and no-op `vercel` strategies. `reactive` and `realtime` strategies are exempt. If a compact strategy's `recap + tail` exceeds 1200 chars (highly likely given `targetTokens: 1000` ≈ 4000 chars), it is allowed to show more context to the judge than the `off` baseline, creating a biased result.
- **Tighter than Budget:** The 1200-char (≈300 token) cap is significantly tighter than the 1000-token budget used for compaction. This handicaps the `off` strategy far beyond a "fair" production limit.

## 3. S12 Critical Logic Bug: DOESNT_FIX / CRITICAL
The "Missing Middle" bug identified in R2 persists in `runner.ts` and `mini-bench-judge.ts`. 
- **The Gap:** When compacting at `turn 8` with `keepTail: 2`, the summarizer is told to keep turns 7 and 8. The summarizer (presumably) returns a summary of turns 1–6. The benchmark then constructs the visible context starting from the `lastCompactionPoint` (turn 8).
- **Result:** Turns 7 and 8 are **NEITHER** in the summary (they were kept) **NOR** in the reconstructed tail (which starts at 8). They are permanently lost from the judge's view. This makes `reactive` strategies fail on the most recent facts before compaction.

## 4. S13 Logic Mismatch: REGRESSED
The divergence between `runner.ts` (deterministic) and `mini-bench-judge.ts` (ensemble) is now wider:
- `runner.ts` has NO context-window simulation (it provides an "oracle" view for `off`).
- `runner.ts` uses `Math.max(last, current - keepTail)` for its tail logic, while `mini-bench-judge.ts` uses `slice(last, current)`.
- Deterministic metrics and Ensemble verdicts on the same fixture are now non-comparable.

## 5. Additional New Flaws (S15+)
- **S15 (Whitespace Destruction):** `flushText` calls `.trim()`, which destroys intentional leading/trailing whitespace in fixture content.
- **S17 (Over-truncation):** The `simulateContextWindow` logic for finding a turn boundary looks **forward** from the cut point. If a very long turn follows the cut, it can discard hundreds of additional characters just to find the next newline, resulting in a window much smaller than requested.

### Final Verdict: REGRESSED
**REGRESSED**. The R3 fixes over-corrected surface issues (S9/S14) while introducing systemic unfairness and widening the architectural gap between the two judge paths. The benchmark currently fails to provide an "honest head-to-head" comparison.
```
