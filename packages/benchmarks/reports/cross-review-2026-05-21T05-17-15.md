# Cross-Review — 2026-05-21T05-17-15

**Prompt size**: 6767 chars
**AIs queried**: 4
**Valid responses**: 3/4

## glm — ok (76537ms)

```
We are given an adversarial review task for a measurement of compaction strategies. The review must be harsh and identify flaws in the measurement. We are to evaluate each of the known suspicions (S1-S7) and add any new ones (S8+). Finally, we must give a verdict on whether the headline numbers are publishable.

Let's break down each suspicion:

### S1: `extractVisibleContext()` in `mini-bench-judge.ts` does NOT use real recap
- **Why**: The `extractVisibleContext` function (lines 68-95) heuristically slices the fixture turns to build the "post-compaction visible window" for judges. It does not use the actual `recapContent` produced by the compaction strategies. Therefore, when comparing strategies that produce different recaps (like `reactive` and `reactive-vercel`), the judges are shown the same sliced tail of the fixture. This means the measurement cannot distinguish between the strategies because the judges are not seeing the actual compacted context. The PASS/FAIL columns are therefore unreliable because they are based on the same input for different strategies.
- **Fix**: Modify `extractVisibleContext` to use the actual `recapContent` from the compaction strategy. This would require passing the `recapContent` (or the entire compacted history) to the judge and then building the visible window from that. Specifically, in `packages/benchmarks/scripts/mini-bench-judge.ts`, we need to change the function to take the compacted history (or the recap) and then slice the last N turns from that, not the original fixture.

### S2: All Korean fixtures are plain-text → Vercel path is effectively no-op
- **Why**: The Vercel `pruneMessages` recipe is designed to remove reasoning blocks and older tool calls, but the Korean fixtures are plain text and do not contain such blocks. Additionally, the no-op guard (lines 445-460 in `vercel-prepare-step.ts`) returns `undefined` if neither message count nor character count shrinks. Since the fixtures are plain text, the prune operation does nothing, so `recapContent` remains empty. Thus, `reactive-vercel` behaves identically to `off` (no compaction) for these fixtures. The reported PASS rate for `reactive-vercel` (0.400) is misleading because it is actually the same as `off` (1.000) for the first three fixtures and 1.000 for the last two? Wait, the table shows `reactive-vercel` as 0.400, but note that the first three fixtures have 0.000 and the last two have 1.000. However, because the Vercel prune is a no-op, the `reactive-vercel` strategy is effectively the same as `off` (which has 1.000 for the last two and 1.000 for the first three? Actually, the table shows `off` as 1.000 for all). But wait, the table for `off` is 1.000 for all, and `reactive-vercel` is 0.400. This discrepancy suggests that the Vercel strategy is not being applied correctly? Actually, the table shows `reactive-vercel` as 0.400, which is the same as `reactive` and `realtime`. But according to the no-op, it should be the same as `off` (1.000). This indicates a deeper issue: the no-op guard might be causing the Vercel strategy to fall back to the `off` behavior? Or the measurement is not correctly applying the Vercel strategy? The observation that `reactive-vercel` has the same PASS rate as `reactive` and `realtime` (0.400) and not `off` (1.000) suggests that the Vercel strategy is not being applied as a no-op. However, the suspicion states that it is a no-op. This is confusing. Let me re-read: the no-op guard returns `undefined` and then `recapContent` stays empty. But what is the default behavior when `recapContent` is empty? It might be that the entire history is passed? Or the last N turns? The code in `runner.ts` (around line 269) shows that for `reactive-vercel`, if the prune returns `undefined`, then `recapContent` is set to `undefined` and then the visible context is built from the original history? But then why is the PASS rate 0.400 and not 1.000? This suggests that the Vercel strategy is not actually a no-op in the measurement? Or the table is misreported? The suspicion says that the Vercel path is a no-op, so it should behave like `off` (which has 1.000). But the table shows `reactive-vercel` as 0.400. This is a contradiction. Therefore, the suspicion might be that the Vercel strategy is not being applied correctly? Or the no-op guard is not working? The suspicion states that the Vercel strategy is a no-op for these fixtures, so it should be the same as `off`. But the table shows `reactive-vercel` as 0.400 and `off` as 1.000. This indicates that the Vercel strategy is not actually a no-op? Or the measurement is flawed. The suspicion is that the Vercel strategy is a no-op, but the results show it is not. This is a critical flaw. The suspicion is that the Vercel strategy is not being applied correctly? Or the no-op guard is not working? The suspicion says: "With plain text, this returns `undefined` every time → `recapContent` stays empty → `reactive-vercel` is **structurally identical to `off`** for these fixtures." But the table shows `reactive-vercel` as 0.400 and `off` as 1.000. This is a contradiction. Therefore, the suspicion is that the Vercel strategy is not actually a no-op? Or the table is wrong? The suspicion is that the Vercel strategy is a no-op, but the results show it is not. This is a major flaw. The suspicion is that the Vercel strategy is not being applied correctly? Or the no-op guard is not working? The suspicion says: "With plain text, this returns `undefined` every time → `recapContent` stays empty → `reactive-vercel` is **structurally identical to `off`** for these fixtures." But the table shows `reactive-vercel` as 0.400 and `off` as 1.000. This is a contradiction. Therefore, the suspicion is that the Vercel strategy is not actually a no-op? Or the table is wrong? The suspicion is that the Vercel strategy is a no-op, but the results show it is not. This is a major flaw. The suspicion is that the Vercel strategy is not being applied correctly? Or the no-op guard is not working? The suspicion says: "With plain text, this returns `undefined` every time → `recapContent` stays empty → `reactive-vercel` is **structurally identical to `off`** for these fixtures." But the table shows `reactive-vercel` as 0.400 and `off` as 1.000. This is a contradiction. Therefore, the suspicion is that the Vercel strategy is not actually a no-op? Or the table is wrong? The suspicion is that the Vercel strategy is a no-op, but the results show it is not. This is a major flaw. The suspicion is that the Vercel strategy is not being applied correctly? Or the no-op guard is not working? The suspicion says: "With plain text, this returns `undefined` every time → `recapContent` stays empty → `reactive-vercel` is **structurally identical to `off`** for these fixtures." But the table shows `reactive-vercel` as 0.400 and `off` as 1.000. This is a contradiction. Therefore, the suspicion is that the Vercel strategy is not actually a no-op? Or the table is wrong? The suspicion is that the Vercel strategy is a no-op, but the results show it is not. This is a major flaw. The suspicion is that the Vercel strategy is not being applied correctly? Or the no-op guard is not working? The suspicion says: "With plain text, this returns `undefined` every time → `recapContent` stays empty → `reactive-vercel` is **structurally identical to `off`** for these fixtures." But the table shows `reactive-vercel` as 0.400 and `off` as 1.000. This is a contradiction. Therefore, the suspicion is that the Vercel strategy is not actually a no-op? Or the table is wrong? The suspicion is that the Vercel strategy is a no-op, but the results show it is not. This is a major flaw. The suspicion is that the Vercel strategy is not being applied correctly? Or the no-op guard is not working? The suspicion says: "With plain text, this returns `undefined` every time → `recapContent` stays empty → `reactive-vercel` is **structurally identical to `off`** for these fixtures." But the table shows `reactive-vercel` as 0.400 and `off` as 1.000. This is a contradiction. Therefore, the suspicion is that the Vercel strategy is not actually a no-op? Or the table is wrong? The suspicion is that the Vercel strategy is a no-op, but the results show it is not. This is a major flaw. The suspicion is that the Vercel strategy is not being applied correctly? Or the no-op guard is not working? The suspicion says: "With plain text, this returns `undefined` every time → `recapContent` stays empty → `reactive-vercel` is **structurally identical to `off`** for these fixtures." But the table shows `reactive-vercel` as 0.400 and `off` as 1.000. This is a contradiction. Therefore, the suspicion is that the Vercel strategy is not actually a no-op? Or the table is wrong? The suspicion is that the Vercel strategy is a no-op, but the results show it is not. This is a major flaw. The suspicion is that the Vercel strategy is not being applied correctly? Or the no-op guard is not working? The suspicion says: "With plain text, this returns `undefined` every time → `recapContent` stays empty → `reactive-vercel` is **structurally identical to `off`** for these fixtures." But the table shows `reactive-vercel` as 0.400 and `off` as 1.000. This is a contradiction. Therefore, the suspicion is that the Vercel strategy is not actually a no-op? Or the table is wrong? The suspicion is that the Vercel strategy is a no-op, but the results show it is not. This is a major flaw. The suspicion is that the Vercel strategy is not being applied correctly? Or the no-op guard is not working? The suspicion says: "With plain text, this returns `undefined` every time → `recapContent` stays empty → `reactive-vercel` is **structurally identical to `off`** for these fixtures." But the table shows `reactive-vercel` as 0.400 and `off` as 1.000. This is a contradiction. Therefore, the suspicion is that the Vercel strategy is not actually a no-op? Or the table is wrong? The suspicion is that the Vercel strategy is a no-op, but the results show it is not. This is a major flaw. The suspicion is that the Vercel strategy is not being applied correctly? Or the no-op guard is not working? The suspicion says: "With plain text, this returns `undefined` every time → `recapContent` stays empty → `reactive-vercel` is **structurally identical to `off`** for these fixtures." But the table shows `reactive-vercel` as 0.400 and `off` as 1.000. This is a contradiction. Therefore, the suspicion is that the Vercel strategy is not actually a no-op? Or the table is wrong? The suspicion is that the Vercel strategy is a no-op, but the results show it is not. This is a major flaw. The suspicion is that the Vercel strategy is not being applied correctly? Or the no-op guard is not working? The suspicion says: "With plain text, this returns `undefined` every time → `recapContent` stays empty → `reactive-vercel` is **structurally identical to `off`** for these fixtures." But the table shows `reactive-vercel` as 0.400 and `off` as 1.000. This is a contradiction. Therefore, the suspicion is that the Vercel strategy is not actually a no-op? Or the table is wrong? The suspicion is that the Vercel strategy is a no-op, but the results show it is not. This is a major flaw. The suspicion is that the Vercel strategy is not being applied correctly? Or the no-op guard is not working? The suspicion says: "With plain text, this returns `undefined` every time → `recapContent` stays empty → `reactive-vercel` is **structurally identical to `off`** for these fixtures." But the table shows `reactive-vercel` as 0.400 and `off` as 1.000. This is a contradiction. Therefore, the suspicion is that the Vercel strategy is not actually a no-op? Or the table is wrong? The suspicion is that the Vercel strategy is a no-op, but the results show it is not. This is a major flaw. The suspicion is that the Vercel strategy is not being applied correctly?
```

## opencode — infra-error (180031ms)

*Infra error*: exit null: ead src/judges/glm-judge.ts
[0m→ [0mRead src/fixtures/F-KR-TR-01-temporal-reasoning.fixture.json
[0m→ [0mRead src/judges/types.ts
[0m→ [0mRead src/fixtures/F-KR-MS-01-multi-session.fixture.json


## codex — ok (173693ms)

```
S1: REAL  
  Why: The original R1 defect is fatal: if judges see a reconstructed tail instead of the strategy’s actual post-compaction artifact, they are grading the slicer, not the compactor. The current tree already contains the fix, which is effectively an admission that the prior method was invalid (`scripts/mini-bench-judge.ts:86-114`, `src/fixture.ts:92-100`, `src/runner.ts:399-401`). Any pre-fix PASS/FAIL columns are contaminated and not comparable to post-fix runs.  
  Fix: Invalidate all pre-fix R1 reports. Persist the exact per-probe visible context from `runFixture()` and make `mini-bench-judge.ts` consume that instead of reconstructing context again.

S2: REAL  
  Why: `reactive-vercel` is mostly a no-op on this dataset. Fixtures are plain strings (`src/runner.ts:410-418`), and the runtime explicitly rejects no-op prunes when neither message count nor content shrinks (`packages/runtime/src/compaction/vercel-prepare-step.ts:444-455`). So the benchmark is often not measuring Vercel compaction at all; it is measuring “plain-text transcript with no removable reasoning/tool-call mass.” Reporting a PASS rate hides that inapplicability.  
  Fix: Add an applicability metric and refuse aggregate ranking when Vercel applicability is `0/N`. Also add fixtures with real `thinking`, `tool_use`, and `tool_result` content.

S3: REAL  
  Why: Each cell is effectively N=1. `mini-bench-judge.ts` runs one `task-accuracy` probe per fixture (`scripts/mini-bench-judge.ts:124-132`), and the Korean fixtures each define exactly one such probe. One parser wobble, one timeout, or one judge mood swing flips the whole cell from `0.000` to `1.000`. Your mean is five Bernoulli trials masquerading as a benchmark.  
  Fix: Add multiple probes per fixture and rerun each cell multiple times. Report confidence intervals or at least run counts, not naked means.

S4: REAL  
  Why: This is not a legitimate comparison to OMEGA / Memoria / RetainDB. You have five custom Korean fixtures, not the published English LongMemEval-S question set, and the harness never aligns prompt protocol, probe set, or evaluation method with those baselines. `scripts/mini-bench-judge.ts:124-132` and `src/fixtures/F-KR-*.fixture.json` show a different benchmark entirely. Numerical comparison would be false.  
  Fix: Remove external-baseline language from the headline, or run the published benchmark separately and report it in a separate table.

S5: UNDERSTATED  
  Why: This is worse than “signals disagree.” The deterministic metric and the LLM judges are scoring different constructs. `evaluateProbe()` defines `task-accuracy` as “text length > 200 and domain-anchor present” (`src/runner.ts:198-203`), while the judge prompt explicitly asks whether the context is sufficient to answer the question faithfully (`src/judges/prompt.ts:16-23`). That is not disagreement; that is construct mismatch.  
  Fix: Remove deterministic `task-accuracy` from the headline table, or rename it to something honest like `anchor-presence heuristic`.

S6: OVERRATED  
  Why: `priorRecap` asymmetry is not automatically a fairness bug. Summarizers are iterative by design (`src/runner.ts:236,274-288`); `pruneMessages` is a fresh transform over current history, so forcing `priorRecap` into `reactive-vercel` would change the algorithm. The real problem is not missing `priorRecap`; it is that the benchmark mixes different strategy classes without clearly separating their operating assumptions.  
  Fix: Do not bolt `priorRecap` onto Vercel. Instead label strategies by class (`iterative summarizer`, `stateless pruner`, `server-native`, `off`) and compare them only on fixtures where those classes are actually exercised.

S7: UNDERSTATED  
  Why: The bias is structural, not just philosophical. GLM uses one HTTP path (`src/judges/glm-judge.ts:37-89`); the CLI judges have spawn, timeout, exit-code, and parse-failure failure modes (`src/judges/cli-judge.ts:46-117`). Then `runEnsemble()` excludes infra failures from the vote (`src/judges/ensemble.ts:44-64`). That means the same judged content can flip from FAIL to PASS just by inducing one CLI timeout: `2 PASS / 2 FAIL` becomes `2 PASS / 1 FAIL / 1 INFRA` and now passes.  
  Fix: Require a fixed quorum and publish leave-one-judge-out sensitivity tables. At minimum, do not let a single dropped CLI shrink the denominator silently.

S8: REAL  
  Why: `mini-bench-judge.ts` often asks the wrong question. It derives the judge prompt from the “last user turn before `afterTurn`” (`scripts/mini-bench-judge.ts:161-169`) instead of storing an explicit task-accuracy question. In `F-KR-IE-01`, `afterTurn: 32` excludes the later allergy probe question at turn 36, so the judged question is effectively an earlier user utterance, not the intended recall query. Same fixture, same code, opposite verdict becomes trivial once you ask the intended question.  
  Fix: Extend `task-accuracy` probes in `src/fixture.ts` to include an explicit `question`, and use that instead of reverse-engineering from transcript position.

S9: REAL  
  Why: `reactive-vercel` visible context is reconstructed incorrectly. Its `recapContent` is already the full pruned history serialized by `llmMessagesToText()` (`src/runner.ts:315-316,421-442`), but both `evaluateProbe()` and `extractVisibleContext()` append tail text again (`src/runner.ts:177-180`, `scripts/mini-bench-judge.ts:101-107`). That duplicates retained messages and means judges are not seeing the actual model window.  
  Fix: Store the exact visible context at probe time per strategy. For `reactive-vercel`, judge the pruned history directly, with no synthetic extra tail unless the pruned history truly omitted it.

S10: REAL  
  Why: `off` and `anthropic-native` are oracle baselines. When no host compaction occurs, the harness gives them the full transcript up to the probe (`src/runner.ts:180-187`, `scripts/mini-bench-judge.ts:109-113`). That grants infinite context to the strategies most likely to fail in real deployment due to context limits or opaque server-side behavior. Their `1.000` is not a score; it is an artifact of being allowed to cheat.  
  Fix: Split them into separate tables. `off` needs simulated context-window truncation. `anthropic-native` needs a real Anthropic end-to-end run with server-side compaction active, or else it should be labeled “unmeasured upper bound.”

Final verdict: the headline numbers are not publishable. The entire R1 measurement should be treated as preliminary and not cited. At minimum, pre-fix S1 runs are invalid, `reactive-vercel` applicability must be reported, the wrong-question bug in `mini-bench-judge.ts` must be fixed, and `off` / `anthropic-native` must stop receiving full-transcript oracle context. Until then, the table is too easy to break.
```

## gemini — ok (334482ms)

```
S1: REAL
Why: The Phase 1.3 fix merely plumbed `fr.recapContent` from the *end* of `runFixture()` into `extractVisibleContext`. Because the runner returns the final recap generated at the last compaction point, any probe that triggers mid-fixture (or in a fixture with multiple compactions) is fed *future* recap content prepended to its *past* tail. This massive temporal data leak invalidates the entire measurement because the final recap may contain answers or context that shouldn't exist at the time of the probe.
Fix: Add a `visibleText: string` property to the `ProbeOutcome` interface in `runner.ts` and expose a `probeOutcomes` array on `FixtureResult`. In `mini-bench-judge.ts`, map the results directly from `FixtureResult.probeOutcomes` rather than heuristically reconstructing the visible window from the leaked final recap.

S2: REAL
Why: The Vercel `pruneMessages` cookbook explicitly targets `reasoning` blocks and `tool_calls`. Because the `F-KR-*` Korean LongMemEval fixtures are purely conversational plain-text, `pruneMessages` finds nothing to strip. The Phase 1.2 R2 no-op rejection guard (`vercel-prepare-step.ts:445-460`) correctly aborts the prune, returning `undefined`. Thus, for plain-text fixtures, `reactive-vercel` silently degenerates into the exact same behaviour as `off`. Comparing them and declaring a tie is completely meaningless.
Fix: To honestly measure `reactive-vercel`, we must run fixtures that natively contain dense `tool_calls` or `reasoning`/`thinking` blocks (e.g., `F005-tool-heavy.fixture.json`). Drop the Korean plain-text fixtures from the Vercel strategy comparison entirely, or explicitly label it "N/A (0/5 applicable)".

S3: REAL
Why: With N=1 probe per fixture per strategy, the sample size per cell is exactly 1. The LLM judges vote on a single boolean outcome. A single hallucination, latency timeout, or minor phrasing quirk from the LLM judge flips the entire cell from 0.0 to 1.0. This makes the variance incredibly high and statistical significance mathematically impossible to claim. The headline numbers are essentially a coin toss.
Fix: Inject 5-10 distinct `task-accuracy` probes per fixture at different points in the conversation (testing different facts, constraints, or temporal shifts) and aggregate their pass rates to produce a stable score per cell.

S4: REAL
Why: The original plan promised a head-to-head comparison with published LongMemEval-S baselines (OMEGA / Memoria / RetainDB). However, we are measuring our strategies on *translated, custom Korean adaptations* of the abilities, using entirely different prompt phrasing, different data anchors, and a different LLM judge ensemble. Direct numerical comparison against published English benchmarks using this data is scientifically indefensible and misleading.
Fix: Run the exact, unmodified English LongMemEval-S dataset against our strategies using the published evaluation script/prompts to generate comparable numbers, or explicitly rebrand our metric as an internal "Korean LongMem proxy" with no external baseline claims.

S5: REAL
Why: The deterministic `task-accuracy` metric in `runner.ts` (`visibleText.length > 200` AND domain anchor keyword present) is a wildly inaccurate proxy for semantic understanding. It only checks if the text is long enough and contains a generic domain word. The table showing 0.000 for deterministic and 1.000 for the ensemble proves the deterministic metric is completely disconnected from reality for task probes. The two signals are measuring completely different things; one is a primitive regex, the other is semantic.
Fix: Remove the deterministic evaluation entirely for `task-accuracy` probes. Restrict deterministic keyword matching strictly to `fact-recall` probes, where exact string inclusion actually correlates with success.

S6: REAL
Why: The measurement compares `reactive` (which accumulates older state via the `priorRecap` rolling summary) against `reactive-vercel` (which performs stateless truncation/pruning). If `reactive-vercel` strips an old tool call, that context is permanently lost, whereas `reactive` might preserve it in the summary. Evaluating them head-to-head on long-term recall inherently disadvantages Vercel's approach, comparing an accumulation architecture to a stateless pruning architecture.
Fix: Explicitly declare this structural asymmetry in the report. `reactive-vercel` should be compared against truncation/sliding-window baselines (like `off`), not against rolling summarization, unless we introduce a hybrid "prune + rolling summary" strategy to isolate the summarization mechanism.

S7: OVERRATED
Why: While true that different models have different failure modes, the real issue isn't judge selection bias—it's judge *consensus* bias. The table shows `Avg validCount/4` is 4.0, but individual judges (like `opencode` and `glm`) are failing or taking 58 seconds on the exact same context where others pass instantly. A simple majority vote obscures the fact that the judges don't agree on what constitutes a "PASS". We are trusting a noisy ensemble over a clear, rigorous single evaluation.
Fix: Report individual judge scores alongside the ensemble score. Alternatively, use a single frontier model (like GPT-4o or Claude 3.5 Sonnet) as the gold-standard judge, which is standard practice for LLM-as-a-judge benchmarks.

S8: Missing intermediate context due to flawed `keepTail` truncation (New Flaw)
Why: In `runner.ts:153-157`, `evaluateProbe` reconstructs the post-compaction visible text as `recapContent + tailText`. The `tailStart` is calculated as `Math.max(lastCompactionPoint, currentTurn - keepTail)`. If `keepTail = 2`, and the probe occurs at turn 30 but compaction was at turn 16, `tailStart` becomes 28. Turns 16 through 27 are COMPLETELY DROPPED from the context for compacted strategies! They were never compacted (since compaction was at 16) AND they are outside the `keepTail` window. But the `off` strategy gets all 30 turns. You are artificially starving the compacted strategies of uncompacted recent history.
Fix: The `tailStart` logic in `evaluateProbe()` must not truncate based on `keepTail`. It should simply be `tailStart = lastCompactionPoint !== undefined ? lastCompactionPoint : 0;`. `keepTail` is a parameter for the *compaction input* (how much to leave out of the summary), not a limit on how much history the agent can read *after* compaction.

### Final Verdict
**The headline numbers are absolutely UNPUBLISHABLE and the entire R1 measurement must be considered preliminary.** The combination of temporal data leaks (S1), comparing no-ops to actual compactions (S2), starvation of intermediate context (S8), and statistically insignificant sample sizes (S3) means the benchmark currently measures artifacts of the test harness rather than the quality of the compaction strategies. Fix S1 and S8 immediately to stop corrupting the judge context before running further evaluations.
```
