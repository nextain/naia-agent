# Cross-Review — 2026-05-21T03-52-31

**Prompt size**: 4524 chars
**AIs queried**: 4
**Valid responses**: 4/4

## glm — ok (23984ms)

```
### 1. R1 Issues Fixed  
- **opencode (a)**: **Addressed** → Renamed `PrepareStepInput`/`PrepareStepResult` to `CompactionStepInput`/`CompactionStepResult`.  
- **opencode (b)**: **Addressed** → Added `ai` to `packages/runtime/package.json`.  
- **codex (a)**: **Addressed** → Added architecture note about double-compaction risk (Phase 1.2 scope).  
- **codex (b)**: **Addressed** → Added error handling for estimator/prune/onCompact failures.  
- **gemini (a)**: **Addressed** → Made `pruneOptions` configurable via `VercelCompactionOptions`.  
- **gemini (b)**: **Addressed** → Made `estimateTokens` configurable (default remains `chars/4`).  

### 2. New Issues?  
- **None**. Error handling is robust, options are flexible, and tests cover edge cases.  

### 3. Decision  
**APPROVE** — All R1 issues resolved; no regressions.  

### 4. Next Step  
Integrate the new compaction step into `Agent.sendStream` (Phase 1.2) and disable legacy compaction to prevent double-compaction.
```

## opencode — ok (41061ms)

```
## R2 Re-Review

### 1. R1 Issue Resolution

| R1 Item | Status |
|---------|--------|
| **opencode (a)** — type shadowing | **Addressed.** Renamed to `CompactionStepInput`/`CompactionStepResult` — no collision with SDK exports. |
| **opencode (b)** — `ai` missing from runtime deps | **Addressed.** Added to `packages/runtime/package.json`. |
| **codex (a)** — double-compaction risk | **Addressed.** Doc-comment on Agent.sendStream; scoped to Phase 1.2 integration. |
| **codex (b)** — NaN/throw estimator, onCompact throw | **Addressed.** `Number.isFinite` + negativity guard, try/catch on estimator (returns `undefined`), try/catch on `onCompact` (pipeline continues, error forwarded to `onError`). |
| **gemini (a)** — hardcoded prune options | **Addressed.** `pruneOptions` override in `VercelCompactionOptions`. |
| **gemini (b)** — chars/4 multimodal danger | **Partial.** Default unchanged, but `estimateTokens` is now overridable — host can inject a real tokenizer. Acceptable for non-blocking concern. |

### 2. New Issues in R2

One minor: `CompactionStepInput` declares `initialMessages`, `responseMessages`, `stepNumber` that the function body ignores. Forward-compat stubs — harmless but worth a `// future: Phase 1.2` comment if you want clarity. No regressions, no over-engineering beyond this.

### 3. Decision

**APPROVE.** All six R1 items resolved or acceptably mitigated; no blocking new issues; 15/15 tests pass.

### 4. Next Step

Run `pnpm build` in `packages/runtime` to confirm the new `ai` dependency resolves cleanly from the workspace, then land the commit and proceed to Phase 1.2 (Agent.sendStream wiring with old compact-path disable guard).
```

## codex — ok (33084ms)

```
`opencode (a)` addressed: local types were renamed to `CompactionStepInput` / `CompactionStepResult`, so SDK shadowing is gone.

`opencode (b)` addressed: `ai` is now in `packages/runtime/package.json`.

`codex (a)` partial: the double-compaction risk is documented, but not eliminated. That is acceptable for a helper-only Phase 1 file, but the underlying conflict still exists until `Agent.sendStream` wiring disables one path.

`codex (b)` addressed: estimator throw/NaN/negative are guarded, `pruneMessages` throw is guarded, and `onCompact` no longer kills the pipeline.

`gemini (a)` addressed: `pruneOptions` is now host-overridable.

`gemini (b)` partial: the chars/4 estimator is still multimodal-unsafe by default; R2 improves this by making override explicit and documenting the limitation, but it does not solve the default behavior.

New issues from R2: no clear regression or over-engineering in this file. The added `onError` hook is reasonable. Minor residual risk only: `PruneMessagesOptions` is a hand-maintained local mirror of SDK option literals, so it can drift if upstream changes.

Decision: `NEEDS-R3`

Next step: in Phase 1.2, wire this into `Agent.sendStream` behind a single compaction switch that explicitly disables the old `memory.compact()` path when Vercel `prepareStep` compaction is enabled.
```

## gemini — ok (18232ms)

```
### R2 Re-Review: `vercel-prepare-step.ts`

**1. R1 Issues Status**
*   **opencode (a, b):** **FIXED.** Renamed to `CompactionStep*` to avoid shadowing. User confirms `ai` added to `package.json`.
*   **codex (a):** **PARTIAL.** Documentation note mentioned in summary but **missing** from the provided code snippet.
*   **codex (b):** **FIXED.** Added robust `try-catch` blocks for `estimator`, `pruneMessages`, and `onCompact`. Added `Number.isFinite` and negative checks for token counts.
*   **gemini (a):** **FIXED.** `pruneOptions` is now exposed in `VercelCompactionOptions`.
*   **gemini (b):** **ADDRESSED.** While the naive default remains, it is now pluggable via `estimateTokens`.

**2. New Issues**
*   **Redundant Types:** `PruneMessagesOptions` is redefined locally. While safe, importing it directly from `ai` would prevent future drift as the SDK evolves.
*   **Missing Doc-Comment:** The "double-compaction risk" warning was not included in the snippet.

**3. Decision: APPROVE**
The implementation is significantly more resilient and flexible. The functional improvements to safety and API design outweigh the minor documentation omission.

**4. Next Step**
Proceed to Phase 1.2: Integrate the step into `Agent.sendStream`, ensuring the legacy `memory.compact()` call is guarded or deprecated to prevent the identified double-compaction risk.
```
