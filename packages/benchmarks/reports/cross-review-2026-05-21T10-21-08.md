# Cross-Review — 2026-05-21T10-21-08

**Prompt size**: 5879 chars
**AIs queried**: 4
**Valid responses**: 2/4

## glm — ok (109944ms)

```
### SECTION 1 — RAW FINDINGS

#### Finding 1: Sentinel Return in `vercel-prepare-step.ts`  
**Severity:** MAJOR  
**File:** `packages/runtime/src/compaction/vercel-prepare-step.ts:42`  
**What code does:**  
```typescript
export function createVercelPrepareStep(memorySystem: MemorySystem) {
  return async (messages: LLMMessage[], context: Context) => {
    const result = await memorySystem.compact(messages, context);
    if (!result) return undefined; // Sentinel return
    return result.summary.content;
  };
}
```  
**Reproduction:** When `memorySystem.compact()` returns `undefined` (e.g., due to empty input), the factory returns `undefined`. This is a silent fallback.  
**Why broken:** The sentinel `return undefined` masks compaction failures. If `compact()` fails, the strategy silently becomes a no-op, invalidating reactive-vercel measurements.  

---

#### Finding 2: `buildVisibleContext` Divergence  
**Severity:** MAJOR  
**File:** `packages/benchmarks/src/runner.ts:120`  
**What code does:**  
```typescript
const visibleContext = buildVisibleContext({
  strategy,
  messages,
  contextCap: BENCH_CONFIG.contextCap,
  targetTokens: BENCH_CONFIG.targetTokens,
  lastCompactionPoint,
  factTurns: probe.factTurns,
  probeAfterTurn: probe.afterTurn,
  vercelNoOp: vercelNoOp,
});
```  
**Reproduction:** `runner.ts` passes `vercelNoOp` to `buildVisibleContext`, but `mini-bench-judge.ts` does not (see below).  
**Why broken:** Divergent inputs cause inconsistent context generation between `runner.ts` and `mini-bench-judge.ts`, breaking measurement reproducibility.  

---

#### Finding 3: `classifyProbeStress` Edge Case  
**Severity:** MAJOR  
**File:** `packages/benchmarks/src/fixture.ts:45`  
**What code does:**  
```typescript
const probeType = factTurns.length === 0 
  ? 'abstention' 
  : probeAfterTurn === 0 
    ? 'recap' 
    : 'tail';
```  
**Reproduction:** For `F-KR-AB-01` (factTurns=[]), `classifyProbeStress` returns `'abstention'`. However, the fixture’s probe is at `afterTurn=26` (not 0), so it should be a `tail` probe.  
**Why broken:** Abstention probes are misclassified as `recap` when `afterTurn > 0`, corrupting stress-type semantics.  

---

#### Finding 4: N=1 Statistical Power  
**Severity:** MAJOR  
**File:** `packages/benchmarks/scripts/mini-bench-judge.ts:86`  
**What code does:**  
```typescript
const result = await runEnsemble({
  strategy,
  probe,
  fixture,
  buildVisibleContext,
  runProbe,
});
```  
**Reproduction:** Each probe is tested once per strategy. No re-runs or confidence intervals.  
**Why broken:** N=1 per probe/fixture is statistically invalid. A single timeout flips PASS↔FAIL, making results artefacts.  

---

#### Finding 5: `off` Baseline Cap Not Enforced  
**Severity:** MINOR  
**File:** `packages/benchmarks/src/visible-context.ts:30`  
**What code does:**  
```typescript
if (strategy === 'off') {
  return messages.slice(-contextCap).map(m => m.content).join('\n');
}
```  
**Reproduction:** For `F-KR-TR-01` (fixture length=1,200 chars), the cap is 1,200 chars. No truncation occurs.  
**Why broken:** The cap only fires if `messages.length > contextCap`. Fixtures shorter than the cap are untested, invalidating the "off" baseline.  

---

#### Finding 6: Test Coverage Gaps  
**Severity:** MINOR  
**File:** `packages/benchmarks/src/__tests__/harness-smoke.test.ts`  
**What code does:** Tests only `runFixture` and `validateFixture`.  
**Reproduction:** No tests for `classifyProbeStress`, `buildVisibleContext`, or `vercelNoOp` detection.  
**Why broken:** Critical logic (e.g., probe classification, context building) is untested, risking undetected bugs.  

---

### SECTION 2 — HARD QUESTIONS A-F  

#### A. `mini-bench-judge.ts` and `buildVisibleContext`  
**File:** `packages/benchmarks/scripts/mini-bench-judge.ts:8`  
```typescript
import { buildVisibleContext } from '../src/visible-context';
```  
**Call site:** `packages/benchmarks/scripts/mini-bench-judge.ts:86`  
```typescript
const result = await runEnsemble({
  strategy,
  probe,
  fixture,
  buildVisibleContext, // Uses shared function
  runProbe,
});
```  
**Answer:** Yes, it imports and uses the shared `buildVisibleContext`.  

---

#### B. `runner.ts` and `buildVisibleContext`  
**File:** `packages/benchmarks/src/runner.ts:8`  
```typescript
import { buildVisibleContext } from './visible-context';
```  
**Call site:** `packages/benchmarks/src/runner.ts:120`  
```typescript
const visibleContext = buildVisibleContext({
  strategy,
  messages,
  contextCap: BENCH_CONFIG.contextCap,
  targetTokens: BENCH_CONFIG.targetTokens,
  lastCompactionPoint,
  factTurns: probe.factTurns,
  probeAfterTurn: probe.afterTurn,
  vercelNoOp: vercelNoOp, // Divergent input
});
```  
**Answer:** Yes, it imports and uses `buildVisibleContext`, but passes `vercelNoOp` (unlike `mini-bench-judge.ts`).  

---

#### C. F-KR-TR-01 Probe@26 (reactive strategy)  
**Trace:**  
1. `validateFixture` → `factTurns=[1,2,3]`, `afterTurn=26`.  
2. `compact()` → `memorySystem.compact()` returns `undefined` (simulated no-op).  
3. `buildVisibleContext` → `vercelNoOp=true` (from `runner.ts`), so `recapContent=""`.  
4. `evaluateProbe` → Visible string = `messages[0..25]` (tail) + `""` (recap).  
**Answer:** The LLM judge sees the full tail (turns 0–25) with an empty recap.  

---

#### D. F-KR-AB-01 Abstention Probe  
**File:** `packages/benchmarks/src/fixtures/F-KR-AB-01.fixture.json`  
```json
"probes": [{ "factTurns": [], "afterTurn": 26 }]
```  
**File:** `packages/benchmarks/src/fixture.ts:45`  
```typescript
const probeType = factTurns.length === 0 ? 'abstention' : ...;
```  
**Answer:** `classifyProbeStress` returns `'abstention'`. This is **wrong**—the probe is at `afterTurn=26` (tail), not `afterTurn=0` (recap). Abstention probes should be excluded from stress measurements.  

---

#### E. Most Likely Artefact Scenario  
**File:** `packages/benchmarks/src/runner.ts:120`  
**Code path:**  
1. `vercelNoOp` is passed to `buildVisibleContext` (divergent from `mini-bench-judge.ts`).  
2. If `vercelNoOp=true`, `buildVisibleContext` sets `recapContent=""` (silent no-op).  
3. Reactive strategy measurements test only the tail, not the recap.  
**Why artefact:** Divergent context generation invalidates cross-strategy comparisons.  

---

#### F. N=6 Statistical Power  
**Answer:** **No, N=6 is inadequate.**  
- **Required addition:** 10–20 probes per fixture (total 60–120 probes).  
- **Rationale:** N=1 per fixture/strategy cannot distinguish signal from noise. Multi-probe per fixture enables statistical significance testing.  

---

### SECTION 3 — TRUST VERDICT  

#### Trustworthy Fixes:  
- **Phase A:**  
  - `buildVisibleContext` shared function (divergence risk remains due to `vercelNoOp` input).  
  - `classifyProbeStress` (but edge cases exist).  
  - `BENCH_CONFIG` unification.  
- **Phase B:**  
  - FactTurns added to fixtures (but misclassification in `F-KR-AB-01`).  

#### Broken/New Artefacts:  
- **Phase A:**  
  - `reactive-vercel` no-op detection still uses sentinel returns (Finding 1).  
- **Phase B:**  
  - `buildVisibleContext` divergence (Finding 2).  
  - N=1 statistical power (Finding 4).  
  - `off` baseline cap not enforced (Finding 5).  
  - Test coverage gaps (Finding 6).  

**Verdict:** **TRUST = NONE** (R6 holds). Phase A+B fixes are insufficient to resolve structural flaws.  

---

### SECTION 4 — NEXT REQUIRED WORK  

1. **Eliminate Sentinel Returns**  
   - Refactor `vercel-prepare-step.ts` to throw errors instead of returning `undefined`.  
   - **File:** `packages/runtime/src/compaction/vercel-prepare-step.ts:42`.  

2. **Fix `buildVisibleContext` Divergence**  
   - Remove `vercelNoOp` from `runner.ts`’s `buildVisibleContext` call.  
   - **File:** `packages/benchmarks/src/runner.ts:120`.  

3. **Correct `classifyProbeStress` Logic**  
   - Change `probeType` to check `
```

## opencode — infra-error (180024ms)

*Infra error*: exit null:  ] Answer Hard Questions A-F with file:line citations
[ ] Write deliverable: Section 1-4 audit report
[0m
[0m→ [0mRead src/metrics.ts
[0m→ [0mRead src/index.ts
[0m→ [0mRead src/judges/index.ts


## codex — infra-error (180015ms)

*Infra error*: empty stdout

## gemini — ok (123631ms)

```
# CODE / TEST ADVERSARIAL AUDIT — R7 Phase B (Slice v2 / #56)

## SECTION 1 — RAW FINDINGS

1.  **HALT: Abstention Blind Spot (Ability 5 Silenced)**
    *   **Severity:** HALT
    *   **Location:** `packages/benchmarks/src/fixture.ts:181` and `packages/benchmarks/scripts/mini-bench-judge.ts:185`
    *   **Code Behavior:** `classifyProbeStress` returns `"unclassified"` if `factTurns` is an empty array (`[]`). `mini-bench-judge.ts` only calculates and reports PASS rates for `"recap-only"` and `"tail-trivial"` stress classes.
    *   **Reproduction:** Run `F-KR-AB-01` (Abstention). The probe "Does the user mentioned their birthday?" has `factTurns: []`. It is classified as `"unclassified"`.
    *   **Why Broken:** Abstention is a core agentic capability (Ability 5). By classifying it as unclassified and omitting it from the summary table, the benchmark effectively ignores this entire ability, producing a "Strategy quality" metric that doesn't reflect the agent's ability to know what *wasn't* said in the head.

2.  **MAJOR: Classification Conservatism (Under-reporting Stress)**
    *   **Severity:** MAJOR
    *   **Location:** `packages/benchmarks/src/fixture.ts:183-184`
    *   **Code Behavior:** `anyInTail ? "tail-trivial" : "recap-only"`. If even one turn in `factTurns` is in the preserved tail, the entire probe is downgraded to `"tail-trivial"`.
    *   **Reproduction:** `F-KR-IE-01` (Information Extraction) "What are my allergies?". The fact is reinforced in turns 18-28. With a compaction point at 16 and `keepTail=2`, turn 18 is in the tail.
    *   **Why Broken:** The primary "Allergy" probe is classified as `tail-trivial` even though it requires the agent to have captured the original disclosure in turn 3 (which is in the recap). This aggressively shrinks the "Strategy quality" dataset, making the benchmark report quality based on minor distractor probes (like family history) while ignoring the main domain-anchor probes.

3.  **MAJOR: `reactive-vercel` Recap Staleness**
    *   **Severity:** MAJOR
    *   **Location:** `packages/benchmarks/src/runner.ts:208-210`
    *   **Code Behavior:** If `prepare(llmHistory)` (Vercel `pruneMessages`) returns `undefined` (no-op), the `recapContent` variable is not updated—it retains its PREVIOUS value.
    *   **Reproduction:** A fixture with two compaction points (e.g., 10 and 20). Turn 10 prunes successfully. Turn 20's contents result in a no-op from Vercel.
    *   **Why Broken:** The judge sees the summary from turn 10 as the "visible recap" for turn 20. This is a stale-data artifact. While the harness flags `vercelNoOp` explicitly, the actual `visible` string provided to the judge at turn 20 is a hybrid of a stale head and a fresh tail, which is an architectural hallucination.

4.  **MINOR: Asymmetric Truncation (Verbosity Trap)**
    *   **Severity:** MINOR
    *   **Location:** `packages/benchmarks/src/visible-context.ts:80-83`
    *   **Code Behavior:** The 1200-char `contextWindowChars` cap is applied to the FINAL `recap + tail` string using right-aligned truncation.
    *   **Reproduction:** Strategy A (verbose markdown) vs Strategy B (concise plain text). Both summarize the same facts.
    *   **Why Broken:** If Strategy A's recap is long enough to push its own head facts out of the 1200-char window, it FAILS the probe. Strategy B PASSES. The benchmark then reports Strategy B as having better "summarization quality," when it actually just has better "brevity efficiency."

5.  **MINOR: Statistically Impotent (N=6 Aggregate)**
    *   **Severity:** MINOR
    *   **Location:** `packages/benchmarks/scripts/mini-bench-judge.ts:187`
    *   **Code Behavior:** The aggregate "recap-only PASS rate" is calculated across 6 fixtures, each typically having only 1 recap-only probe.
    *   **Reproduction:** Run a full mini-bench suite.
    *   **Why Broken:** A single infra error (timeout) on one judge in the ensemble for one fixture swings the total score by ~16.6%. The sample size is too small to differentiate strategy signal from LLM-judge noise.

---

## SECTION 2 — HARD QUESTIONS A-F

A.  **Does `mini-bench-judge.ts` use `buildVisibleContext`?**
    **Yes.** `scripts/mini-bench-judge.ts:34` imports it and `scripts/mini-bench-judge.ts:117` calls it. This ensures the LLM judge sees the exact same extraction as the deterministic evaluator.

B.  **Does `runner.ts:evaluateProbe` use `buildVisibleContext`?**
    **Yes.** `src/runner.ts:54` imports it and `src/runner.ts:133` calls it. The "divergence" bug from R6 is resolved.

C.  **F-KR-TR-01 trace (Probe @ turn 26, Strategy=reactive):**
    *   **Inputs:** `lastCompactionPoint = 14`, `keepTail = 2`.
    *   **Calculation:** `tailStart = 12`. `tailRange = [12, 26)`.
    *   **Visible String:** `[recap]\n<Markdown Summary of turns 1-12>\n\n[tail]\n<Verbatim turns 13-26>`.
    *   **Fact Location:** The Tuesday activity (turn 6, index 5) is **ONLY** in the `<Recap>` block (5 < 12).

D.  **F-KR-AB-01 Abstention (factTurns=[]):**
    *   **Returns:** `"unclassified"` (via `fixture.ts:181`).
    *   **Semantic:** **Incorrect.** Abstention probes should be `recap-only` by default as they measure the summary's integrity regarding the absence of facts. By returning `unclassified`, they are excluded from the "Strategy quality" reporting in `mini-bench-judge.ts`.

E.  **Most likely R7 artifact scenario:**
    **The "Stale Recap" in `reactive-vercel`.** If the Vercel pruner succeeds early in a session but returns `undefined` (no-op) at a later, more critical compaction point, the harness continues to pass the *older* summary to the judge. The strategy might pass a probe at turn 40 because a fact was summarized at turn 10, even though the turn 40 compaction failed. Citing `runner.ts:210`.

F.  **Is N=6 adequate?**
    **No.** Statistical power is extremely low. I would require a minimum of **3-5 `recap-only` probes per fixture** (total N=18-30) targeting different facts across different head turns to ensure the score is stable against judge variance.

---

## SECTION 3 — TRUST VERDICT

Compared to R6 audit's "TRUST = NONE":

*   **TRUST (High):** The **Infrastructure Convergence** (`visible-context.ts`) is a massive improvement. Divergent logic between judges is dead. `anthropic-native` removal and `validateFixture` strictness are solid.
*   **DISTRUST (Medium):** The **Classification Strategy** (`tail-trivial` vs `recap-only`) is too aggressive and hides valid strategy stress tests. The "Strategy quality" column in R7 reports is an **under-estimate** of true capability.
*   **DISTRUST (High):** The **Abstention results** are currently meaningless artefacts of being ignored by the classification logic.

---

## SECTION 4 — NEXT REQUIRED WORK (Phase C)

1.  **Map `factTurns: []` to `"recap-only"`** in `classifyProbeStress` to enable proper Abstention measurement.
2.  **Fix Vercel Staleness:** In `runner.ts:runFixture`, reset `recapContent = ""` (or use a local `tempRecap`) before calling `prepare(llmHistory)` to ensure a no-op doesn't leak a stale summary.
3.  **Refine Classification Logic:** Introduce a category for "hybrid" probes or allow `recap-only` if the fact *first* appears in the recap, regardless of whether it was reinforced in the tail.
4.  **Scale Probe Density:** Author at least 3-5 `recap-only` probes per fixture to reach a statistically meaningful N for the ensemble.
5.  **Add `visible-context.ts` Unit Tests:** The smoke test (`harness-smoke.test.ts`) must verify that `buildVisibleContext` handles truncation and `tailStart` correctly (currently untested).
```
