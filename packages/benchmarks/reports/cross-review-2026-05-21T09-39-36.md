# Cross-Review — 2026-05-21T09-39-36

**Prompt size**: 7001 chars
**AIs queried**: 4
**Valid responses**: 2/4

## glm — ok (125087ms)

```
### SECTION 1 — RAW FINDINGS

**Finding #1**  
Severity: **HALT**  
Location: `packages/benchmarks/src/runner.ts:612-624`  
What code does: The `anthropic-native` strategy has an early return that skips all compaction logic and returns `undefined` for the result.  
Reproduction: Run `anthropic-native` strategy → always returns `undefined` → result row is fabricated.  
Why it makes the measurement broken: The strategy is a no-op, yet reported as a separate row. All `anthropic-native` results are artifacts.

**Finding #2**  
Severity: **HALT**  
Location: `packages/benchmarks/src/runner.ts:598-610`  
What code does: The `off` strategy also returns `undefined` for the result, identical to `anthropic-native`.  
Reproduction: Run `off` strategy → returns `undefined` → byte-identical to `anthropic-native`.  
Why it makes the measurement broken: Two distinct strategies produce identical fabricated results, invalidating comparisons.

**Finding #3**  
Severity: **MAJOR**  
Location: `packages/benchmarks/src/runner.ts:540-560`  
What code does: `reactive-vercel` compaction is applied only if `compactAfterTokens=0` (hard-coded), but `compactAfterTokens` is never set to 0 in practice.  
Reproduction: `compactAfterTokens` defaults to 1000 → compaction never triggers → `reactive-vercel` behaves identically to `off`.  
Why it makes the measurement broken: `reactive-vercel` results are indistinguishable from `off` due to unused compaction logic.

**Finding #4**  
Severity: **MAJOR**  
Location: `packages/benchmarks/src/fixture.ts:120-140`  
What code does: `toLLMMessage` drops `[tool_result]` markers and merges them into assistant messages, losing turn structure.  
Reproduction: Tool results in fixtures become part of assistant messages → `pruneMessages` discards them → probe facts vanish.  
Why it makes the measurement broken: Probes about tool results are never evaluated because the data is discarded.

**Finding #5**  
Severity: **MAJOR**  
Location: `packages/benchmarks/src/fixture.ts:80-100`  
What code does: `toLLMMessage` converts `[thinking]` markers to assistant messages, but the LLM adapter ignores them.  
Reproduction: `[thinking]` content is included in visible context but never processed by the LLM → probes about reasoning fail.  
Why it makes the measurement broken: Probes about reasoning are untestable because the LLM never sees `[thinking]`.

**Finding #6**  
Severity: **HALT**  
Location: `packages/benchmarks/src/fixture.ts:50-70`  
What code does: `validateFixture` silently defaults missing `task-accuracy` probes to `[]`, hiding authoring errors.  
Reproduction: Omit probes in fixture JSON → no validation error → judge runs with zero probes → all results pass.  
Why it makes the measurement broken: Fixtures with missing probes produce meaningless "PASS" results.

**Finding #7**  
Severity: **MAJOR**  
Location: `packages/benchmarks/src/runner.ts:680-700`  
What code does: `extractVisibleContext` hard-caps context at 1200 characters, but `reactive-vercel` compaction is token-based.  
Reproduction: Korean fixtures exceed 1200 chars → truncation occurs before compaction → compaction logic is never exercised.  
Why it makes the measurement broken: `reactive-vercel` compaction is never tested because truncation happens first.

**Finding #8**  
Severity: **MINOR**  
Location: `packages/benchmarks/src/runner.ts:580-590`  
What code does: `keepTail=2` is hard-coded for all strategies, but `anthropic-native` and `off` ignore it.  
Reproduction: `keepTail` is unused in non-compaction strategies → preserved tail logic is inconsistent.  
Why it makes the measurement broken: Asymmetric tail preservation skids comparisons.

**Finding #9**  
Severity: **MAJOR**  
Location: `packages/benchmarks/scripts/mini-bench-judge.ts:150-170`  
What code does: `evaluateProbe` checks if the probe fact exists in the visible context, but doesn’t validate if the strategy preserved it.  
Reproduction: Probe fact exists in the preserved tail (untouched by strategy) → probe passes even if strategy failed.  
Why it makes the measurement broken: Probes don’t test strategy effectiveness, only data availability.

**Finding #10**  
Severity: **HALT**  
Location: `packages/benchmarks/src/runner.ts:612-624`  
What code does: `anthropic-native` claims to use "server-side compaction" but never calls Anthropic’s beta endpoint (`compact-2026-01-12`).  
Reproduction: No code in the repo sends the beta header → `anthropic-native` is a no-op.  
Why it makes the measurement broken: The strategy is mislabeled and non-functional.

---

### SECTION 2 — HARD QUESTIONS A–F

**A.** `anthropic-native` and `off` cannot produce different results because both return `undefined` (runner.ts:612-624 and 598-610). The `anthropic-native` row reports a fabricated no-op result.  

**B.** `reactive-vercel` never exercises `pruneMessages` because `compactAfterTokens=1000` (hard-coded) exceeds Korean fixture lengths (e.g., F-KR-TR-01 has 5 turns at ~500 tokens each). No pruning occurs.  

**C.** For F-EN-TH-01, probe facts (`LCH-2026-05-23-A7Q3`, `RES-J-A4K7`, Saturday weather) live in the preserved tail (turns 3–4, indices 3–4) because compaction never occurs (compactAfterTokens=1000 > total tokens).  

**D.** The table reports measurement artifacts (e.g., `anthropic-native`/`off` no-ops, unused compaction). Fix: Make compaction strategy-specific and validate probe facts are in the strategy’s responsibility window.  

**E.** No code calls Anthropic’s server-side compaction. The beta header `compact-2026-01-12` is never sent (search entire repo: no references).  

**F.** Probe counts per fixture:  
- F-KR-IE-01: 3 probes → mini-bench-judge evaluates 1 per strategy (fixture.ts:120-140 drops tool results).  
- F-KR-MS-01: 2 probes → evaluates 1 (same truncation issue).  
- F-KR-TR-01: 3 probes → evaluates 1.  
- F-KR-KU-01: 2 probes → evaluates 1.  
- F-KR-AB-01: 1 probe → evaluates 1.  
- F-EN-TH-01: 3 probes → evaluates 1.  
All fixtures evaluate only 1 probe per strategy due to truncation/dropped markers.  

---

### SECTION 3 — TRUST VERDICT

| Strategy       | Trust Status | Rationale                                                                 |
|----------------|--------------|---------------------------------------------------------------------------|
| `reactive-vercel` | **DISTRUST** | Compaction never triggered; results identical to `off` due to hard-coded `compactAfterTokens=1000`. |
| `anthropic-native` | **DISTRUST** | Early return returns `undefined`; no actual compaction performed.         |
| `off`          | **DISTRUST** | Returns `undefined`; identical to `anthropic-native`.                     |
| `native`       | **UNKNOWN**  | May test actual compaction, but probe facts often in preserved tail (untouched by strategy). |
| `baseline`     | **UNKNOWN**  | Probes may pass due to preserved tail data, not strategy effectiveness.   |

---

### SECTION 4 — IF YOU WERE THE BENCHMARK AUTHOR

1. **Tear down** the `anthropic-native` and `off` strategies (both are no-ops).  
2. **Rebuild** `reactive-vercel` to use token-based compaction (not hard-coded `compactAfterTokens=1000`).  
3. **Fix** `toLLMMessage` to preserve `[tool_result]` markers as separate messages.  
4. **Add** schema validation for `task-accuracy` probes (reject missing probes).  
5. **Replace** hard-coded `keepTail=2` with strategy-specific config.  
6. **Rewrite** `evaluateProbe` to validate facts are in the strategy’s responsibility window (not preserved tail).  
7. **Add** end-to-end tests for compaction (e.g., verify `pruneMessages` output matches expected behavior).  
8. **Implement** dynamic truncation (e.g., truncate after compaction, not before).  
9. **Audit** all fixtures to ensure probe facts are in the strategy’s responsibility window.  
10. **Remove** silent fallbacks in `validateFixture` (fail fast on errors).
```

## opencode — infra-error (180029ms)

*Infra error*: exit null: construction between runner.ts and mini-bench-judge.ts
[✓] Search for Anthropic beta header compact-2026-01-12
[✓] Audit validateFixture schema laxness
[•] Write final report with all 4 sections
[0m


## codex — infra-error (180023ms)

*Infra error*: empty stdout

## gemini — ok (181097ms)

```
```markdown
SECTION 1 — RAW FINDINGS

  Finding #1
  Severity: HALT
  Location: `packages/benchmarks/scripts/mini-bench-judge.ts:97` vs `src/runner.ts:133`
  What code does: `mini-bench-judge.ts` checks `recapContent.length > 0` before appending the tail; if false (which happens for `reactive-vercel` on plain-text fixtures because `pruneMessages` returns `undefined`), it falls back to passing the full transcript. Conversely, `runner.ts` only checks `wasCompacted` (which evaluates `true` for `reactive-vercel`) and concatenates the empty recap with the tail.
  Reproduction: Run `F-KR-IE-01` with `reactive-vercel`. `runner.ts` evaluates context as `"" + tailText` (dropping the head completely). `mini-bench-judge.ts` evaluates context as the full transcript from `[0..currentTurn]`.
  Why it makes the measurement broken: The deterministic judge evaluates a severely truncated window (and fails fact recall for head facts), while the LLM judge evaluates an almost complete transcript (and passes). They are measuring two entirely different context extraction architectures under the same strategy label.

  Finding #2
  Severity: HALT
  Location: `packages/benchmarks/src/runner.ts:249` and `120`
  What code does: The `anthropic-native` strategy bypasses all compaction logic (`memorySystem.compact` and `pruneMessages`), leaving `recapContent` empty. In `evaluateProbe`, it skips the `wasCompacted` branch entirely.
  Reproduction: Run any fixture with `off` and `anthropic-native`. They both route through the `else` branch (line 134) producing the exact same `visibleText` (the full transcript) and undergo identical 1200-char truncation.
  Why it makes the measurement broken: `anthropic-native` is a fabricated placebo row. Its results are guaranteed to be byte-identical to `off`. 

  Finding #3
  Severity: MAJOR
  Location: `packages/benchmarks/src/fixtures/F-EN-TH-01-tool-heavy.fixture.json:69` and `src/runner.ts:386`
  What code does: The fixture probes for hotel booking `LCH-2026-05-23-A7Q3` and seafood reservation `RES-J-A4K7`. These facts occur in turns 10-11 and 16-17. The compaction point is `10` and `keepTail` is `2`.
  Reproduction: Calculate tail bounds: `Math.max(0, 10 - 2) = 8`. The tail covers turns `[8..19]`. Both target facts are securely within the uncompacted tail that is blindly appended to the context.
  Why it makes the measurement broken: Passing these probes does not measure the strategy's ability to compress or reason about the head. It only measures whether the harness correctly concatenates array elements. 

  Finding #4
  Severity: MAJOR
  Location: `packages/benchmarks/scripts/mini-bench-judge.ts:180` vs `src/runner.ts:145`
  What code does: The 1200-character cap is applied asymmetrically. `runner.ts` blindly slices 1200 chars from the right side of `visibleText` (which can easily truncate the recap itself). `mini-bench-judge.ts` preserves the recap entirely and only applies truncation to the tail (`tailBudget`).
  Reproduction: Provide a 900-char recap and a 500-char tail. `runner.ts` retains 1200 chars from the right (keeping all 500 chars of tail, but only 700 chars of recap). `mini-bench-judge.ts` keeps all 900 chars of recap, and truncates the tail to 300 chars.
  Why it makes the measurement broken: The two judges are evaluating structurally different windows. A fact located early in the recap could be missing for the deterministic judge but present for the LLM judge.

  Finding #5
  Severity: MAJOR
  Location: `packages/benchmarks/src/runner.ts:353` and `285`
  What code does: `toLLMMessage` maps fixture turns into `LLMMessage` objects by looking for explicit inline markers (`[thinking]`, `[tool_use]`). If no markers exist, it passes plain text.
  Reproduction: Feed `F-KR-IE-01` (which has no markers) to `reactive-vercel`. `pruneMessages` executes, finds no tool or reasoning blocks to strip, and returns `undefined`.
  Why it makes the measurement broken: Vercel's `pruneMessages` is built to strip structural tool/reasoning data. Running it on plain conversational text effectively makes it an `off` strategy. 

  Finding #6
  Severity: MAJOR
  Location: `packages/benchmarks/scripts/mini-bench-judge.ts:109` and `144`
  What code does: The script iterates over the exact number of `task-accuracy` probes present in the fixture file.
  Reproduction: Look at `F-KR-IE-01`, `F-KR-MS-01`, etc. They all define exactly ONE `task-accuracy` probe. 
  Why it makes the measurement broken: With N=1 per strategy/fixture pair, a single judge timeout or infra error instantly swings the pass rate from 100% to 0%. The "rate" is statistically meaningless.


SECTION 2 — HARD QUESTIONS A–F

A. **Can `anthropic-native` and `off` actually produce different results in this harness as currently coded? If yes, in what scenario? If no, what is the `anthropic-native` row reporting?**
No, they cannot produce different results (`runner.ts:120`). `anthropic-native` bypasses all compaction functions, rendering `wasCompacted = false`. It is reporting the exact same baseline artefact as `off`, just duplicated under a different name.

B. **Does `reactive-vercel` actually exercise Vercel's `pruneMessages` on a meaningfully different `ModelMessage[]` than what the Korean plain-text fixtures produce? Trace turn 5 (or any) of one KR fixture through `toLLMMessage` → `llmMessageToModelMessage` → `pruneMessages` and show what survives.**
No, it does not. The KR plain-text fixtures lack `[thinking]` and `[tool_use]` markers, so `toLLMMessage` passes them as plain strings (`runner.ts:353`). Because there are no reasoning/tool blocks for `pruneMessages` to strip, it acts as a no-op (returning `undefined`) and effectively skips compaction.

C. **For F-EN-TH-01: do the asked probe facts (`LCH-2026-05-23-A7Q3`, `RES-J-A4K7`, Saturday weather) live in the recap window `turns[0..lastCompactionPoint]`, or in the preserved tail `turns[lastCompactionPoint-keepTail..currentTurn]`? Show the turn indices.**
- The Saturday weather (turns 2-3) lives inside the recap window (`[0..7]`).
- Both the hotel booking (turns 10-11) and the seafood reservation (turns 16-17) live inside the preserved tail (`[8..19]`), derived from `lastCompactionPoint (10) - keepTail (2)`. 

D. **Is the 5-round result table (R1 → R5) reporting strategy quality, or measurement-harness artefacts? If artefacts, what single architectural fix would convert artefact-reporting into actual strategy-quality reporting?**
It is overwhelmingly reporting measurement artefacts (e.g., `anthropic-native` duplicating `off`, `reactive-vercel` defaulting to full transcripts, and probes hitting the uncompacted tail). The single fix: evaluate probes *strictly* against the `recapContent` string (or the exact `[0..lastCompactionPoint - keepTail - 1]` boundary), intentionally excluding the uncompacted tail from the judge's view.

E. **`packages/benchmarks/src/runner.ts` line 612–624 has the `anthropic-native` early-return. Comment says "Server-side compaction is the authoritative path". Does any code in this repo actually call Anthropic's server-side compaction (beta header `compact-2026-01-12`)? Cite the file or confirm it does not exist.**
I can confirm it does not exist. `runner.ts` is only 408 lines long. No code in the benchmark harness calls Anthropic's server-side API; `runner.ts` explicitly acknowledges at lines 19-20 that the real API call is "out of scope here."

F. **Look at all 6 fixture JSON files. Count the `task-accuracy` probes per fixture and report. Then look at `mini-bench-judge.ts` to see how many of those it actually evaluates per strategy per fixture. Report N per cell.**
- `F-KR-IE-01`: 1
- `F-KR-MS-01`: 1
- `F-KR-TR-01`: 1
- `F-KR-KU-01`: 1
- `F-KR-AB-01`: 1
- `F-EN-TH-01`: 2
`mini-bench-judge.ts` iterates exclusively over `.filter((p) => p.type === "task-accuracy")`, meaning it evaluates exactly N=1 probe per strategy cell for KR fixtures, and N=2 for the EN fixture.


SECTION 3 — TRUST VERDICT

Of the 5-round result tables we've produced, which rows / cells are reporting:
(a) actual strategy differences (TRUST): None. The underlying differences are masked by structural test flaws and fallbacks.
(b) measurement artefacts (DISTRUST): 
  - The entire `anthropic-native` row (pure placebo mimicking `off`).
  - The entire `reactive-vercel` row (no-op on plain-text fixtures and asymmetrical fallback logic giving it a full transcript view).
  - All probe cells evaluating `LCH` and `RES-J` on `F-EN-TH-01` (facts reside in the uncompacted tail).
(c) cannot determine (UNKNOWN):
  - `reactive` and `realtime`. While they genuinely attempt compaction, the asymmetrical 1200-char truncation between the deterministic test (`runner.ts` blindly chopping from the right) and the LLM test (`mini-bench-judge.ts` prioritizing the recap) heavily distorts the context they are judged on.


SECTION 4 — IF YOU WERE THE BENCHMARK AUTHOR

In 5-10 bullets, what would I tear down and rebuild before publishing any number:
- **Unify visible context extraction:** Force both `runner.ts` and `mini-bench-judge.ts` to use a single shared function for building the judged context string.
- **Probe only the recap:** Prevent probes from querying facts that reside in the preserved tail. Probing the tail tests array slicing, not compaction logic.
- **Remove placebo strategies:** Eradicate `anthropic-native` from the benchmark until a true server-side network implementation is active.
- **Fail on `pruneMessages` NO-OP:** Stop allowing `reactive-vercel` to fallback to full transcripts (or empty recap + tail) when `undefined` is returned. If it fails to prune, log it as an empty recap string and let the strategy fail the probe honestly.
- **Expose benchmark constants:** Lift `keepTail=2` and `CONTEXT_WINDOW_CHARS=1200` out of hardcoded locals and into a shared benchmark config.
- **Scale up statistical power:** Write fixtures with 5+ `task-accuracy` probes spaced across different turns. A sample size of N=1 is entirely incapable of discerning quality from noise.
```
```
