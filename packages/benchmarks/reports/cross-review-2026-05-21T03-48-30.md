# Cross-Review — 2026-05-21T03-48-30

**Prompt size**: 3235 chars
**AIs queried**: 4
**Valid responses**: 4/4

## glm — ok (33204ms)

```
### Review Summary

1. **Cookbook Fidelity**: ✅ Faithful. Uses `pruneMessages` with standard options (`reasoning: "all"`, `toolCalls: "before-last-3-messages"`, `emptyMessages: "remove"`), default threshold (100k tokens), and matches the expected signature. Missing `initialMessages`/`responseMessages` usage is acceptable for this reactive step.

2. **Hidden Risks**: ⚠️  
   - **Type Safety**: `ModelMessage` from `ai` SDK is correct, but `readonly` arrays in `PrepareStepInput` are unused—could be simplified.  
   - **Performance**: `JSON.stringify` for token estimation is O(n) and may be slow for large histories. Custom estimators mitigate this.  
   - **Error Handling**: No fallback if `pruneMessages` fails (unlikely but possible).  

3. **Integration Concerns**:  
   - Ensure pruned messages retain critical context (e.g., system instructions) since `reasoning: "all"` removes reasoning messages.  
   - Verify `VercelClient` compatibility—pruned messages must align with SDK expectations.  
   - Monitor `onCompact` callback performance in high-throughput scenarios.  

4. **Missing Tests**:  
   - Edge case: Messages with `role: "system"` to ensure they’re preserved.  
   - Threshold boundary: Token count exactly at `compactAfterTokens`.  
   - Mixed message types (e.g., interleaved tool calls/reasoning).  

5. **Decision**:  
   **APPROVE** — Faithful to Vercel’s pattern, well-typed, and passes tests. Minor optimizations (e.g., unused fields) are acceptable.
```

## opencode — ok (151752ms)

```
**1. Cookbook faithfulness**

`pruneMessages` options are correct — all three parameters match ai@6.0.168's API exactly. Threshold 100k matches cookbook.

**Problem**: Your local `PrepareStepInput`/`PrepareStepResult` types **shadow the SDK's exported types** of the same names but are structurally incompatible. The SDK's `PrepareStepFunction` receives `{ steps, stepNumber, model, messages, experimental_context }` — your type has `initialMessages`/`responseMessages` (not in SDK) and lacks `steps`/`model`/`experimental_context`. The factory output **cannot be directly assigned** to the SDK's `prepareStep` option. If this is intentional (adapter layer), the naming is misleading; if not, it won't type-check at integration.

**2. Hidden risks**

- `ai` is **not in `packages/runtime/package.json`** — it's hoisted from workspace root. Standalone consumers of `@nextain/naia-runtime` will crash.
- `JSON.stringify(messages).length / 4` materializes the full message array as a string — expensive for large tool-result payloads but acceptable for Phase 1.
- No error boundary around `pruneMessages` — a malformed message propagates into the step pipeline unhandled.

**3. Integration concerns**

Since `Agent.sendStream` → `VercelClient` → SDK `streamText`, you'll need an adapter to bridge your custom signature to the SDK's `PrepareStepFunction`. Also verify `pruneMessages` doesn't strip system messages that `VercelClient` injects (persona riders, tool schemas).

**4. Missing tests**

- Exactly-at-threshold boundary (should return `undefined`)
- All messages pruned → empty array
- Messages containing only tool results (common in agent loops)
- `onCompact` callback that throws

**5. REQUEST CHANGES** — rename the local types (e.g. `CompactionStepInput`/`CompactionStepResult`) to stop shadowing the SDK's identically-named exports, and add `ai` to `runtime`'s `package.json` dependencies. These are the two items that will bite at integration.
```

## codex — ok (69801ms)

```
1. Faithfulness: mostly faithful. [vercel-prepare-step.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/runtime/src/compaction/vercel-prepare-step.ts:100) matches the cookbook shape on the important bits: `100_000` default threshold, chars/4 estimator, `pruneMessages({ reasoning: "all", toolCalls: "before-last-3-messages", emptyMessages: "remove" })` at [108-113](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/runtime/src/compaction/vercel-prepare-step.ts:108). The callback signature is acceptable, but the “EXACT cookbook recipe” claim is slightly too strong because you ignore `initialMessages`, `responseMessages`, and `stepNumber` rather than using the SDK’s full callback semantics.

2. Hidden risks: small in the helper, bigger at the boundary. `defaultEstimateTokens()` at [39-40](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/runtime/src/compaction/vercel-prepare-step.ts:39) is cheap enough for now, but `JSON.stringify` can be noisy and provider-inaccurate. No validation for `compactAfterTokens` / estimator output means `NaN`, negative, or thrown errors propagate unexpectedly. `onCompact` can also throw and fail generation.

3. Integration concerns: the real issue is architectural. `Agent.sendStream()` currently compacts through `memory.compact()` before the request, not through Vercel `prepareStep` ([agent.ts:290-305](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/core/src/agent.ts:290), [612-675](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/core/src/agent.ts:612)). If you add this helper without disabling or reconciling that path, you risk double compaction and divergent semantics. Also, `VercelClient` must actually expose a `prepareStep` hook; today that is not proven here.

4. Missing tests: threshold equality (`=== 100_000`), invalid estimator outputs (`NaN`, negative, throw), `onCompact` throw behavior, and one integration test proving `Agent.sendStream` uses exactly one compaction path.

5. Decision: REQUEST CHANGES — helper is fine, but the current patch does not yet prove correct `Agent.sendStream` integration and risks double compaction.
```

## gemini — ok (146823ms)

```
**1. Is the cookbook recipe faithful?**
Yes. The `PrepareStepInput` and `PrepareStepResult` types accurately mirror Vercel AI SDK's `maxSteps` hook signature. The 100k threshold and `pruneMessages` arguments (`reasoning`, `toolCalls`, `emptyMessages`) align perfectly with the official reactive compaction cookbook.

**2. Hidden risks**
- `defaultEstimateTokens` (`JSON.stringify().length / 4`) is dangerously naive for multi-modal inputs (e.g., base64 images) or massive JSON tool results. It risks severe under/overestimation. 
- The `pruneMessages` configuration is hardcoded. If a host needs to retain more tool calls or protect specific message roles, they cannot override it through `VercelCompactionOptions`.

**3. Integration concerns**
- **System Messages:** `pruneMessages` generally assumes system instructions are passed via the separate `system` string in `streamText`. If `naia-agent` injects system prompts as `role: "system"` inside the `messages` array, ensure `pruneMessages` doesn't inadvertently strip or truncate them.
- **Lifecycle:** Ensure `Agent.sendStream` correctly wires this into the `streamText` options (specifically `prepareStep` for multi-step loops).

**4. Missing tests**
- **Un-prunable state:** What happens when a single massive message (e.g., a giant tool result) exceeds the threshold and pruning cannot reduce it below the limit?
- **Sequence integrity:** Ensure pruned output doesn't result in orphaned tool results (tool results without matching tool calls).
- **Multi-modal:** Test the default estimator's behavior on simulated image/buffer inputs.

**5. Decision**
**APPROVE** (Non-blocking: Consider exposing `pruneMessages` overrides and documenting the default estimator's multi-modal limitations before full integration.)
```
