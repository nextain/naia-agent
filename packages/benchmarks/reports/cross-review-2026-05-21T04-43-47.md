# Cross-Review — 2026-05-21T04-43-47

**Prompt size**: 7899 chars
**AIs queried**: 4
**Valid responses**: 4/4

## glm — ok (76764ms)

```
### Verdict: **APPROVE**  
*(With minor feedback on error handling and test coverage)*  

---

### 1. **Layering: Core SDK-Agnosticity** ✅  
**Feedback**:  
- `packages/core` remains SDK-agnostic. The `prepareCompact` hook uses only `LLMMessage` from `@nextain/agent-types` (no SDK types leaked).  
- `packages/runtime` cleanly encapsulates SDK dependencies via `createLLMMessagePrepareCompact` and adapters.  
- **Suggestion**: Add a JSDoc comment to `AgentOptions.prepareCompact` explicitly noting:  
  > *"This hook is provided by `packages/runtime` and may use the Vercel AI SDK internally. Core remains SDK-agnostic."*  

---

### 2. **Double-Compaction Guard** ✅  
**Feedback**:  
- The guard is **correctly enforced** via the early return in `#maybeCompact`:  
  ```ts
  if (this.#strategy === "reactive" && this.#prepareCompact) {
    return this.#runPrepareCompact(); // Skips memory.compact()
  }
  ```  
- `compactedThisSession` flag prevents multiple compactions per turn (verified in tests).  
- `exportHandoff()` path is isolated (test `AVC-07` confirms no double-compaction with `handoffThreshold: 0`).  

---

### 3. **Adapter Lossiness** ⚠️  
**Feedback**:  
- **Missing test case**: `LLMMessage` with `image` source `type === "url"` is not explicitly tested in `ADP-01..10`.  
  - Current tests cover `FilePart` (local files) but not remote URLs.  
- **Risk**: The adapter silently drops `image_url` blocks if `LLMMessage` uses a different structure than `ModelMessage`.  
- **Action**: Add test `ADP-11` for:  
  ```ts
  const llmMsg: LLMMessage = { role: "user", content: [{ type: "image", source: { type: "url", url: "https://..." } }] };
  const modelMsg = llmMessageToModelMessage(llmMsg); // Should preserve URL
  ```  

---

### 4. **Threshold Semantics** ✅  
**Feedback**:  
- Defaulting `compactAfterTokens: 0` is correct. The Agent’s `contextBudget` should be the sole trigger for compaction.  
- Alternative (helper double-gating) would violate surgical changes and add complexity.  

---

### 5. **Error Handling** ⚠️  
**Feedback**:  
- Swallowing errors and skipping fallback is **acceptable** for transient issues (e.g., malformed history).  
- **Risk**: Critical errors (e.g., SDK misconfiguration) could silently disable compaction.  
- **Suggestion**: Augment logging to distinguish transient vs. critical errors:  
  ```ts
  this.#host.logger.warn("agent.compaction.prepareCompact.error", {
    err: String(err),
    severity: err instanceof TypeError ? "critical" : "transient", // Example
  });
  ```  

---

### 6. **Surgical-Changes Check** ✅  
**Feedback**:  
- All changes trace directly to wiring Vercel SDK:  
  - `bin/naia-agent.ts`: Flag wiring.  
  - `packages/core/agent.ts`: Hook integration.  
  - `packages/runtime`: SDK boundary + adapters.  
- `VPS-R2-05` fix is unrelated but necessary (invalid `"keep-all"` → `"none"`).  

---

### Test Coverage Gaps  
1. **Missing test**: `prepareCompact` throwing a **critical error** (e.g., SDK misconfiguration).  
   - Current test `AVC-06` only covers transient errors.  
   - Add test for SDK-specific errors (e.g., invalid `maxTokens`).  
2. **Edge case**: Empty history after pruning.  
   - Test `ADP-10` covers this, but add assertion for `history.length === 0` in `AVC-07`.  

---

### Final Notes  
- **Approve** with minor feedback on error logging and URL image handling.  
- **Test additions**
```

## opencode — ok (139147ms)

```
I'll review the actual code to give a thorough verdict. Let me read all the changed files.
Now let me check the `LLMContentBlock` type and the `image` block's `source` shape, and verify the `createHost` helper used in tests:
Now let me verify the `image` source type handling and one more edge case:
465/465 runtime tests pass (1 skipped, pre-existing). The cli-app failures are the pre-existing GLM/ollama key issues noted in the PR description.

---

## Verdict: **APPROVE**

Clean change. Architecturally sound, well-tested, surgical. Specific observations on each review question:

### 1. Layering — Clean

`core/src/agent.ts` uses only `LLMMessage` from `@nextain/agent-types` in the `prepareCompact` signature (line 125-127). No `ai` SDK types, no default values referencing the SDK, no implicit dependency. The factory lives in `runtime/src/compaction/vercel-prepare-step.ts:390-408` and is injected at the composition root (`bin/naia-agent.ts:618-620`). Layering is correct.

### 2. Double-compaction guard — Correct

The control flow in `#maybeCompact` is:

1. **Line 682-688**: `off` / `anthropic-native` → early return `undefined`
2. **Line 691**: Budget check → return `undefined` if under
3. **Line 697-699**: `if (strategy === "reactive" && prepareCompact) return #runPrepareCompact()` — **early return**, the `memory.compact()` path at lines 702-747 is unreachable
4. Lines 702-747: in-house `memory.compact()` — only reached when strategy is `reactive` WITHOUT `prepareCompact`, or `realtime`

Plus `compactedThisTurn` (line 300/321) prevents any re-entry within the same turn. The `exportHandoff()` path (`memory.compact({keepTail:0})`) is separate and correctly excluded by `handoffThreshold: 0` in the tests. Guard is watertight.

### 3. Adapter lossiness — One gap to note

**`redacted_thinking` dropped**: Intentional, documented at `vercel-prepare-step.ts:227-229`. Good.

**`image` with `source.type === "url"`**: This is silently downgraded. `llmBlockToUserPart` (`vercel-prepare-step.ts:278-284`) reads `block.source.data` (which for `url` type contains the URL string) and emits a `FilePart`. The reverse path (`modelMessageToLLMMessage`, line 357-365) always creates `source.type: "base64"`. A URL image source round-trips as `{ type: "image", source: { type: "base64", data: "https://..." } }` — semantically wrong. **Low severity** since: (a) the adapter is explicitly "lossy by design", (b) VercelClient likely never produces URL-source images (converts to base64 at ingestion), (c) this is pruning context, not archival. But worth a brief doc comment on `llmBlockToUserPart` flagging the `url` case.

**No other `LLMContentBlock` variants are mishandled** — the union is `text | thinking | redacted_thinking | tool_use | tool_result | image`, and each is covered.

### 4. Threshold semantics — Correct design

Factory defaults `compactAfterTokens: 0` (`vercel-prepare-step.ts:394`), meaning the inner helper always fires. Agent's `contextBudget` at `agent.ts:691` is the single authoritative trigger. Hosts can double-gate via explicit `compactAfterTokens` (tested in ADP-09). Single-gate-by-default is the right call — avoids confusion about which threshold owns the decision.

### 5. Error swallowing — Acceptable, consistent

`#runPrepareCompact` catches the throw, logs, returns `undefined` (`agent.ts:661-665`). No fallback to `memory.compact()`. This is **consistent** with the in-house path's error behavior (`agent.ts:744-747` — also catches and returns `undefined`). The test AVC-07 correctly verifies the turn survives and `memory.compact()` is NOT called even on error (the double-compaction guard holds even in the error path).

The trade-off: on persistent `prepareCompact` failure, the agent runs without compaction until provider token limits are hit. Same risk as the in-house path. Acceptable.

### 6. Surgical changes — Clean

Every diff line is traceable to "wire Vercel prune into Agent.sendStream":
- `agent.ts`: `AgentOptions.prepareCompact` + `#prepareCompact` field + `#runPrepareCompact` method + 3-line branch in `#maybeCompact`
- `vercel-prepare-step.ts`: adapter functions + `createLLMMessagePrepareCompact` factory
- `bin/naia-agent.ts`: `--reactive-vercel` flag + conditional spread at both Agent instantiations
- Test files: each test validates a specific guard/behavior of the new wiring
- `index.ts`: 3 new exports

The only non-wiring change is the VPS-R2-05 fix (`toolCalls: "none"` replacing invalid `"keep-all"` in `vercel-prepare-step.test.ts:169-173`). This fixes a bug in a Phase 1.1 test that Phase 1.2 would have masked. Acceptable in the same PR but ideally called out in the commit message.

### Minor suggestions (non-blocking)

1. **Doc the `url` image downgrade** on `llmBlockToUserPart` — one line noting URL-source images lose their type distinction.
2. **VPS-R2-05 fix** deserves a mention in the commit body (it's a correctness fix, not just a test adjustment).
```

## codex — ok (159868ms)

```
**REQUEST_CHANGES**

1. `prepareCompact` can report success without reducing history, which suppresses the existing fallback and can send the same over-budget request anyway. In [agent.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/core/src/agent.ts:655), `#runPrepareCompact()` accepts any non-empty `pruned` array, sets `#compactedThisSession`, emits a `compaction` event, and returns. Because [#maybeCompact](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/core/src/agent.ts:697) short-circuits on the reactive hook, `memory.compact()` never gets a chance afterward. But [createLLMMessagePrepareCompact](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/runtime/src/compaction/vercel-prepare-step.ts:397) treats any non-empty `pruned` result as success, even if it is byte-for-byte identical. With `pruneMessages`, that no-op case is realistic on text-only histories. Result: the guard is preserved, but compaction effectiveness is not; the agent may proceed with an unchanged over-budget request. This needs a “did anything actually shrink / did estimate go down” gate before treating the Vercel path as success, or an explicit fallback to `memory.compact()` when the prune path is a no-op.

2. URL-backed images are corrupted by the adapter round-trip. `LLMImageSource` explicitly allows `type: "url"` in [llm.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/types/src/llm.ts:87), but [llmBlockToUserPart](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/runtime/src/compaction/vercel-prepare-step.ts:276) always maps images to a Vercel `file` part using `data`, and [modelMessageToLLMMessage](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/runtime/src/compaction/vercel-prepare-step.ts:357) always reconstructs them as `source.type: "base64"`. A source URL like `https://...` comes back mislabeled as base64 data. That is silent data corruption, and there is no test for it. Either preserve `url` images explicitly or reject them up front and document that loss.

3. `tool_result.isError` is dropped across the adapter boundary. The core contract carries it in [llm.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/types/src/llm.ts:84), but [llmMessageToModelMessage](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/runtime/src/compaction/vercel-prepare-step.ts:244) always encodes tool results as plain text output, and [modelMessageToLLMMessage](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/runtime/src/compaction/vercel-prepare-step.ts:324) never restores an error bit. The existing Vercel adapter already distinguishes `error-text` vs `text` in [vercel-client.ts](/var/home/luke/alpha-adk/projects/naia-agent-worktrees/slice-compact-v2/packages/providers/src/vercel-client.ts:342). Losing that bit changes model-visible conversation state after compaction and should be fixed or at least tested and documented as intentional loss.

On your specific questions: layering is otherwise clean; `core` stays SDK-agnostic and the new type surface only exposes `LLMMessage`. The double-compaction guard itself is mechanically correct for `sendStream()`; I do not see a path where both `prepareCompact` and `memory.compact()` run in the same turn, and the handoff path is separate. Threshold=0 in the factory is the right default if the agent budget is meant to be the single trigger. The main problem is not the trigger, but that the prune path currently treats no-op pruning as success. The current “swallow and do not fallback” behavior is defensible only if the hook made a real compaction attempt that changed state; with the current no-op acceptance, it is too weak.
```

## gemini — ok (115466ms)

```
I have completed the cross-review for Slice 3-XR-Compact v2 / Phase 1.2 (#56) R1. Based on the provided excerpts and architecture description, here is my verdict and detailed feedback.

### Verdict: **APPROVE**

The implementation is surgically applied, maintains strict architectural layering, and correctly implements the requested "exactly one path" compaction guard. The use of a functional hook to bridge the core and runtime packages is an excellent design choice.

---

### Detailed Feedback

#### 1. Layering & Dependency Management
- **Status**: **Excellent**
- **Analysis**: `packages/core` correctly avoids any direct dependency on the Vercel AI SDK (`ai`). The `prepareCompact` hook uses the internal `LLMMessage` type from `@nextain/agent-types`.
- **Validation**: The injection of the factory-created callback from the `runtime` layer into the `core` layer via `bin/naia-agent.ts` follows the "Interfaces, not dependencies" principle of the project.

#### 2. Double-compaction Guard Correctness
- **Status**: **Correct**
- **Analysis**: The logic in `agent.ts` is watertight:
  ```ts
  if (this.#strategy === "reactive" && this.#prepareCompact) {
    return this.#runPrepareCompact();
  }
  // ... legacy path is unreachable if the above condition is true
  ```
- **Guard Details**: The `#runPrepareCompact` method updates `#compactedThisSession` and returns a compaction event, which (based on the `Agent` loop logic) prevents re-entry within the same turn. The `handoff` path's use of `memory.compact()` is appropriately separate as it occurs at a terminal session transition.

#### 3. Adapter Lossiness
- **Status**: **Acceptable (with documentation caveat)**
- **Redacted Thinking**: Dropping `redacted_thinking` is correct and necessary as there is no standard mapping in the Vercel AI SDK `ModelMessage` schema.
- **Image URL Gap**: I've noted the observation that images with `source.type === "url"` are silently dropped by the adapter (`llmBlockToUserPart` only handling `data`). While base64 is the primary use case, this is a functional limitation.
- **Recommendation**: Add a brief JSDoc comment in `vercel-prepare-step.ts` noting that URL-based image blocks are not preserved during this compaction path.

#### 4. Threshold Semantics
- **Status**: **Correct**
- **Analysis**: Defaulting `compactAfterTokens: 0` in the factory is the right design. It prevents "threshold shadowing" where the SDK helper might skip a pruning that the `Agent` (via its `contextBudget`) has already decided is necessary.

#### 5. Error Swallowing & Fallback
- **Status**: **Correct/Safe**
- **Analysis**: The decision to log and return `undefined` (no compaction) rather than falling back to `memory.compact()` is correct. In an opt-in reactive strategy, falling back to a legacy path upon error could lead to inconsistent history states or unexpected side effects that the host specifically intended to bypass.

#### 6. Karpathy 4원칙 Evaluation
- **Simplicity First**: The functional hook approach is significantly simpler than adding a strategy-aware bridge class.
- **Surgical Changes**: The diff is remarkably clean. The fix for `VPS-R2-05` (`toolCalls: "none"`) is a valid and necessary part of ensuring the integration uses valid SDK literals.

---

### Conclusion
The PR is ready for merge. The 17 new tests (32 total for the feature) provide sufficient confidence in both the adapter correctness and the integration safety.

**Summary of Recommendations**:
- (Non-blocking) Document the image-URL limitation in `llmMessageToModelMessage`.
- (Non-blocking) Ensure `modelMessageToLLMMessage` role folding (system -> assistant) is acceptable for the downstream model's interleaving requirements.

Strategic Intent: Completed the cross-review and provided a detailed analysis of the Slice 3-XR-Compact v2 integration. Verdict is APPROVE.

update_topic(title="Cross-Review Complete", summary="I have finalized the cross-review of Slice 3-XR-Compact v2 / Phase 1.2. My analysis confirms that the implementation strictly adheres to the architectural layering requirements, keeping the core SDK-agnostic. The double-compaction guard is correctly implemented via a mutually exclusive branch in the Agent's compaction logic. While I identified a minor limitation in handling URL-based image blocks, it is acceptable within the current scope. I have provided an APPROVE verdict with specific feedback on layering, safety, and adherence to engineering standards.")
```
